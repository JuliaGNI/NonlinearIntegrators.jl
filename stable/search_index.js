var documenterSearchIndex = {"docs":
[{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Algorithms","page":"Algorithms","text":"Every implemented component, with what it computes, how it is implemented, what it costs, and when to reach for it. The mathematical background is on the Theory page; how to select and combine these is on the Usage page.\n\nThroughout, M is the number of quadrature nodes (11 by default), N the dictionary size, S the number of hidden neurons, and k ≤ S the number of atoms selected so far. All formulas are in the sqrtw-scaled space, where the Euclidean inner product is the quadrature-weighted one.","title":"Algorithms"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#The-greedy-loop","page":"Algorithms","text":"oga_fit is the single implementation shared by all four network integrators. It is deliberately integrator-agnostic: it takes a dictionary specification, an activation, nodes, weights and a target, and returns neuron parameters. It knows nothing about GeometricIntegrator, the parameter cache, or the variational equations, which is what makes it directly testable at any precision.\n\noga_fit(oga, σ, nodes, w, y, nneurons;\n        bias_interval, dict_amount, modulation = nothing, symmetry = NoSymmetry())\n    -> OGAResult\n\nPer step it:\n\nscores every dictionary atom against the current residual (oga_scores!, one mul! against the dictionary — the dominant cost, O(NM));\nmasks atoms blocked by the coherence guard and takes an argmax;\noptionally polishes the winner off-grid (Refined);\nappends the atom's column(s) to the maintained QR, rejecting the atom and continuing to the next-best candidate if it adds no new direction;\nrefits all output weights (oga_solve);\nupdates the residual and blocks atoms too coherent with the winner.\n\nOGAResult carries the neuron parameters together with the diagnostics that make rank behaviour visible: atoms (the selected indices, in order), neurons (how many were actually placed), gains (the lVert g^perprVert_w of each accepted atom — a sequence collapsing towards zero is the fingerprint of the reduced-precision failure), rejected, and the final weighted residual norm.","title":"The greedy loop"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Atoms,-neurons-and-columns","page":"Algorithms","text":"One atom does not always mean one neuron. OGASymmetry declares the mapping, which is what lets the two time-reversible integrators share this loop:\n\nSymmetry Neurons per atom Design columns Output weights Used by\nNoSymmetry 1 1 independent NonLinear_OneLayer_GML, Hardcode_int\nMirrorPairs 2 2 independent Time_reversible_OneLayer\nSharedMirrorPairs 2 1 (their sum) shared Time_Reversible_Hardcode\n\nThe mirror map is (w b) mapsto (-w w + b), which sends sigma(wt + b) to sigma(w(1-t) + b) — a reflection about the midpoint of the step. For SharedMirrorPairs both members carry the same output weight, and that sharing is what actually enforces time-reversal symmetry of the ansatz; with independent weights the pair can drift apart.","title":"Atoms, neurons and columns"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#The-boundary-ansatz","page":"Algorithms","text":"Hardcode_int and Time_Reversible_Hardcode represent the step as\n\nq(t) = (1-t)barq + ttildeq + t(1-t) u(t) \n\nso the network only has to fit what is left after the linear part, and every dictionary atom carries the t(1-t) factor. Both enter through oga_fit's modulation argument (the t(1-t) vector) and a target with the straight line subtracted — no separate code path.","title":"The boundary ansatz"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Dictionaries","page":"Algorithms","text":"","title":"Dictionaries"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[BiasGrid1d](@ref)","page":"Algorithms","text":"{±1} crossed with a uniform grid of dict_amount + 1 biases over the method's bias_interval, giving 2(textttdict_amount+1) atoms.\n\nTheory. Complete for ReLUᵏ: by positive homogeneity the magnitude of w carries no shape information the bias grid and output weight do not already absorb, so only the sign remains. See Theory.\n\nImplementation. The grid comes from NonlinearIntegrators.bias_grid, which generates an integer-indexed range in Float64 and casts once to T. Computing lo:(hi-lo)/n:hi at T instead carries a half-precision trap: Float16(70000) overflows to Inf, the step evaluates to zero, and the range constructor throws ArgumentError: range step cannot be zero. Atom order — the w = -1 block first, then w = +1 — is load-bearing, since argmax breaks ties by first index.\n\nCost. N = 2(textttdict_amount+1).\n\nWhen. The default, and the right choice for ReLUᵏ.","title":"BiasGrid1d"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[WeightBiasGrid2d](@ref)","page":"Algorithms","text":"A genuine 2-D grid: weight_amount + 1 magnitudes spaced logarithmically over 2^textttoctaves1 ldots 2^textttoctaves2, optionally sign-symmetric, crossed with the bias grid.\n\nTheory. For an activation that is not positively homogeneous, lvert w rvert sets the transition's length scale, an independent shape parameter. The weight axis is spaced logarithmically because length scales compare by ratio, not by difference; the default octaves = (-3, 3) spans a factor of 64.\n\nImplementation. NonlinearIntegrators.weight_grid exponentiates an integer-indexed Float64 range and casts once, so an octave outside the range of T saturates predictably rather than overflowing mid-computation. bias_amount overrides dict_amount on the bias axis, which is how the total atom count is held roughly constant while the weight axis is added.\n\nCost. N = n_w cdot (n_b + 1), with n_w = 2(textttweight_amount+1) when signed. Since the greedy step is linear in N, trim bias_amount to compensate.\n\nWhen. ELU, GELU, tanh — any non-homogeneous activation. Setting octaves = (0, 0), weight_amount = 0 recovers BiasGrid1d exactly (asserted in test/unit/oga_kernels.jl), so this is a strict generalisation: neutral for the homogeneous activations and enabling for the smooth ones. Pair with NormalizedProjection — with raw projection the large-lvert wrvert atoms would be ranked by amplitude rather than by fit.","title":"WeightBiasGrid2d"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[AngularGrid](@ref)","page":"Algorithms","text":"Atoms on rays through the origin of (w, b) space: (w b) = r(costheta sintheta) for a uniform grid of angles over 0 2pi) and each radius in radii.\n\nTheory. This is the dictionary greedy approximation theory is stated for — a grid on the unit sphere of mathbbR^d+1. It unifies the two cases: for a homogeneous activation one radius suffices and the set covers the same ridge directions as {±1} × grid, but sampled uniformly in atom space. The bias grid is not: uniform spacing in b at lvert w rvert = 1 concentrates resolution where lvert b rvert is large and the atom is nearly constant on 01, i.e. where it matters least. For a non-homogeneous activation the radius is the length scale.\n\nImplementation. The full circle, not a half circle: sigma(t) and sigma(-t) are different functions, so the sign of w is real shape information even though its scale is redundant. The endpoint is excluded because 2pi wraps onto 0.\n\nCost. N = lverttextttradiirvert cdot (textttamount+1).\n\nWhen. As an alternative to WeightBiasGrid2d for smooth activations, and worth trying for ReLUᵏ too — in the seed study it wins more often than the bias grid does, which the non-uniform-coverage argument above predicts.","title":"AngularGrid"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[Refined](@ref)","page":"Algorithms","text":"A decorator: after the greedy argmax picks a grid atom, its (w, b) are polished off the grid by locally maximising the selection score.\n\nTheory. The grid then only has to identify the right neighbourhood. This decouples accuracy from dictionary size — a few dozen atoms plus refinement can match hundreds of thousands — and since the greedy step is linear in N, that is a large cost saving. It is the standard \"OGA with inner optimisation\".\n\nImplementation. A derivative-free compass search: evaluate the score at (w pm h b) and (w b pm h), step to any improvement, shrink h when none improves, repeat iterations times. Derivative-free on purpose — the score is only piecewise smooth for ReLUᵏ (the kink crosses a quadrature node), and it keeps the activation off the ForwardDiff path entirely, so no Dual tag can leak into the working precision.\n\nThe polished objective is always the normalised score, even under RawProjection. The raw inner product is a ranking heuristic among atoms of comparable norm, not an objective: maximised continuously over (w, b) it rewards growing the atom rather than fitting the residual, and the search would drift to large lvert wrvert while the fit got worse.\n\nCost. 4 cdot textttiterations extra score evaluations per step, each O(M) — negligible against the O(NM) scan.\n\nWhen. Whenever the dictionary is coarse, and as a cheap accuracy win at any size. Note the guarantee is one-step: refinement provably improves the first atom's fit but, by greedy myopia, not necessarily the final quartet.","title":"Refined"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Selection-rules","page":"Algorithms","text":"All three fill a score vector by one mul! of the dictionary against the residual and mark unusable atoms with -1, so argmax skips them. An atom is unusable if its projection or its norm is non-finite, or its norm is below the floor.","title":"Selection rules"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[RawProjection](@ref)","page":"Algorithms","text":"mathrmscore(g) = lvertlangle r grangle_wrvert.\n\nThe default, and what the regression tests pin: normalising before selection changes which neurons are picked and steers the Newton solve into a different — empirically worse — basin. Not scale invariant, which is harmless on the ±1 grid (comparable norms) and wrong on a 2-D grid.\n\nCost. O(NM).","title":"RawProjection"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[NormalizedProjection](@ref)","page":"Algorithms","text":"mathrmscore(g) = lvertlangle r grangle_wrvert  lVert grVert_w.\n\nThe textbook criterion: it measures how much of the residual the atom explains, independently of amplitude, which the output weight absorbs anyway. Exact for the first atom. Mandatory for WeightBiasGrid2d and AngularGrid. Also Hardcode_int's rule, hence the OGA1dNormalized preset.\n\nCost. O(NM); the norms are precomputed once per fit.","title":"NormalizedProjection"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[OrthogonalProjection](@ref)","page":"Algorithms","text":"mathrmscore(g) = lvertlangle r grangle_wrvert  lVert g^perprVert_w, with any atom whose orthogonal part has collapsed refused outright.\n\nThe actual orthogonal-greedy criterion — it maximises the one-step residual reduction exactly (equation (G) on the Theory page) — and the direct fix for the reduced-precision failure.\n\nImplementation. lVert g^perprVert^2 = lVert g rVert^2 - lVert Q^topg rVert^2, so all N deflated norms come from one product of the dictionary against the maintained Q. Because the residual is already orthogonal to the selected span, the numerator is unchanged from NormalizedProjection; only the denominator differs. Subtracting in T can go slightly negative for an atom already fully explained — exactly the atom to reject — so the result is clamped rather than erroring. The min_gain floor defaults to sqrtvarepsilon(T).\n\nCost. one extra O(NMk) product per step, the same order as the score itself.\n\nWhen. At reduced precision, where it guarantees a full-rank selected set. Measured, it is the strongest selection rule in the seed study. Be aware of the trade-off: at Float64 its one-step optimality does not survive greedy myopia, and the rank-gain floor can refuse atoms that would have been usable — end-to-end it helps at 16 bits and can hurt above.","title":"OrthogonalProjection"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Fits","page":"Algorithms","text":"tip: The fit is free, so choose it for robustness alone\nThe selected system is tiny: hatA is M times k with M approx 11 quadrature nodes and k le S le 8 neurons, and the Gram matrix it replaces would be k times k. The only operation that scales with anything large is the greedy selection scan over the dictionary, which is a matrix–vector product plus an argmax and is already precision-robust.So the fit contributes essentially nothing to the runtime, at any of the five factorisations, and there is no performance argument for preferring a cheaper one. Pick on numerical behaviour: the measurements in Studies put the rank-revealing fits ahead at Float64, which is not what a cost-first reading would have suggested.\n\nAll five receive the sqrtw-scaled design matrix hatA (M times k) and target haty, so they minimise the same objective and differ only in the factorisation. oga_solve wraps every one of them with a single guarantee: the result has one entry per column and every entry is finite. Where a factorisation throws on a rank-deficient design or divides by a pivot that survived truncation, it falls back to the ridged solve. Enforcing that once, rather than per fit, means it holds for a new fit by construction.","title":"Fits"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[WeightedQR](@ref)","page":"Algorithms","text":"QR of hatA, re-solved from scratch each step (NonlinearIntegrators.weighted_lstsq). Conditioned on kappa(hat A) rather than kappa(hat A)^2; no Gram matrix, and no ridge unless the plain solve returns non-finite.\n\nThe default, and the fit whose arithmetic the Float64/Float32 regression tests pin.\n\nCost. O(Mk^2) per step.\n\nnote: `\\` can throw, not just mislead\nFor a non-BLAS element type — i.e. at Float16 — Julia's \\ throws SingularException on a rank-deficient matrix rather than returning garbage. Letting that escape would put a SingularException back on the seed path at exactly the precision this fit exists to rescue, so it is caught and routed to the ridged solve.","title":"WeightedQR"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[IncrementalQR](@ref)","page":"Algorithms","text":"Reuses the QR maintained across greedy steps (NonlinearIntegrators.IncrementalQRState): one triangular solve instead of a fresh factorisation.\n\nImplementation. Columns are appended by modified Gram–Schmidt with one reorthogonalisation pass. That second pass is not bookkeeping: plain MGS loses orthogonality in proportion to the condition number — which at Float16 is the whole problem — whereas reorthogonalising once restores it to O(varepsilon(T)) for any conditioning that has not already collapsed.\n\nCost. O(Mk) per step, against O(Mk^2) for a fresh factorisation. It also produces two quantities the loop wants anyway: the appended column's deflated norm (the rank gain) and Q itself, which is what makes OrthogonalProjection one matrix product.\n\nWhen. Pair it with OrthogonalProjection — together they are the \"textbook\" efficient and stable OGA, and it is numerically equivalent to WeightedQR up to rounding.","title":"IncrementalQR"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[PivotedQR](@ref)","page":"Algorithms","text":"Householder QR with column pivoting, truncated where the pivot norm drops below textttrtol times the first pivot (NonlinearIntegrators.pivoted_qr_lstsq).\n\nTheory. Pivoting is what makes it rank-revealing: at each step the column with the largest remaining norm is brought forward, so a numerically dependent column is pushed to the end and detected by its collapsed pivot instead of being solved through. Columns past the detected rank receive a zero coefficient.\n\nImplementation. Hand-rolled, because qr(Â, ColumnNorm()) is LAPACK-only and therefore does not exist at Float16 — the precision the remedy is for. Widening to Float32 to borrow LAPACK would reintroduce the Float64 island in miniature. Column norms are recomputed after each elimination rather than downdated — downdating loses accuracy precisely where the pivot norms collapse, which is the regime the factorisation exists to detect, and at k le 8 columns the recomputation is free; the reflector follows the LAPACK convention (v_1 = 1, unnormalised) to avoid a division that can underflow at half precision.\n\nCost. O(Mk^2) per step, plus the pivot search.","title":"PivotedQR"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[TruncatedSVD](@ref)","page":"Algorithms","text":"Minimum-norm solve through a truncated pseudo-inverse, dropping singular directions with sigma  textttrtolsigma_max (NonlinearIntegrators.truncated_svd_lstsq).\n\nTheory. The most robust of the five — a rank-deficient selected set yields a bounded solution rather than amplified rounding noise — and the one to reach for if a single configuration must work unchanged at every precision. Where PivotedQR zeroes a dependent column, this returns the minimum-norm solution, spreading weight across a duplicated pair; the residual is the same and lVert c rVert is smaller.\n\nImplementation. One-sided Jacobi (NonlinearIntegrators.jacobi_svd): orthogonalise the columns pairwise by plane rotations until mutually orthogonal, at which point the column norms are the singular values. Chosen over bidiagonalisation because it is short, generic in the element type (svd is likewise LAPACK-only), and has high relative accuracy on the small singular values — exactly the ones that decide whether the selected atoms are still independent.\n\nwarning: A `Float16` trap in the convergence test\nThe rotation is skipped when lvertbetarvert le varepsilon(T)sqrtalpha sqrtgamma — deliberately not sqrtalphagamma. The product of two squared column norms overflows Float16 once the norms exceed about 16, at which point the threshold becomes Inf, every column pair tests as \"already orthogonal\", and the routine silently returns the unrotated matrix: wrong singular values, no error raised. This was a real bug, caught by comparing against LAPACK at Float16.\n\nCost. O(Mk^2) per sweep, a handful of sweeps at k le 8.","title":"TruncatedSVD"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#[NormalEquationsFit](@ref)","page":"Algorithms","text":"G c = Phioperatornamediag(w) y with G = hatA^tophatA, with the precision-scaled Tikhonov ridge of NonlinearIntegrators.oga_tikhonov and the Float64 island as independent switches.\n\nThis is the baseline, not a recommendation: forming G squares the condition number. It exists so that \"island vs working precision\" and \"ridge vs no ridge\" are two knobs on one code path that can be ablated, rather than differences buried in forked implementations. NormalEquationsFit(ridge = false, island = true) reproduces the arithmetic of OGA1dNormalEquations.\n\nCost. O(Mk^2 + k^3).\n\nnote: Note\nisland = true deliberately violates the package's precision discipline. It is the thing being measured against — see Precision.","title":"NormalEquationsFit"},{"category":"section","location":"Orthogonal Greedy Algorithm/algorithms/#Guard-rails","page":"Algorithms","text":"These are fields on OGA, orthogonal to the three axes.\n\nNorm floor (norm_guard, default true). Atoms whose weighted norm falls below NonlinearIntegrators.oga_norm_floor — sqrtvarepsilon(T) times the largest atom norm — are unusable, rather than normalised by noise. It replaced a hard-coded absolute 1e-12, which sat below varepsilon(textttFloat32) and so never fired in reduced precision. Atoms with a non-finite norm are always excluded regardless of the flag; that is correctness, not policy — at Float16 a high ReLUᵏ power over a wide bias interval overflows sigma(b)^k, and an Inf norm would sail past a bare n > floor test and then divide to NaN.\n\nCoherence guard (coherence, default true). After an atom is selected, atoms whose weighted-L^2 coherence with it exceeds 1 - sqrtvarepsilon(T) are blocked from future selection, keeping the selected set independent. Inert at Float64/Float32, where distinct atoms are well separated; it only bites at Float16, where many grid biases collapse onto the same value. Computed on the fly as langle g_i g_textbestrangle  (lVert g_irVert lVert g_textbestrVert), which avoids materialising a second dictionary-sized array.\n\nUnused-neuron fill (fill_unused, default true). When the loop runs out of usable atoms before all S neurons are placed, the remainder get distinct, well-separated (w, b) with zero output weight. Without this they would all keep (0, 0), become identical rows of the Newton Jacobian, and trade a rank-deficient seed for a rank-deficient solve — the failure would move rather than go away.\n\nActivation check. oga_fit calls NonlinearIntegrators.oga_check_precision once per fit and throws if σ(::T) is not a T. See Precision.\n\nNeuron-count check. oga_fit calls NonlinearIntegrators.oga_check_neuron_count and throws if nneurons is not a multiple of neurons_per_atom(symmetry) — i.e. if an odd count is requested under MirrorPairs or SharedMirrorPairs. The greedy loop places whole atoms, so an odd count would run one step fewer and leave the last neuron at (0, 0), which the unused-neuron fill cannot repair either since it fills a pair at a time. That is exactly the duplicated-row state the fill exists to prevent, so the count is rejected rather than half-honoured. The two time-reversible integrators enforce the same condition on S at construction.","title":"Guard rails"},{"category":"section","location":"Variational Integrator with Symbolic Expression/VISE/#Basic-Usage","page":"-","text":"At first, we have to load several packages:\n\nusing Symbolics\nusing CompactBasisFunctions\nusing NonlinearIntegrators\nusing QuadratureRules\nusing GeometricProblems\nusing GeometricIntegrators\n\nAdditional package would be required if we want to use Nested Sindy expression.\n\nusing Symbolics\n\nLike what we would do in GeometricIntegrators.jl, we first define the problem we want to solve. The type of problem here is restricted to Lagrangian ordinary equations, i.e. models have a regular Lagrangian and its Lagrangian equation of motions.\n\nT = 100.0\nh_step = 2.0\nHHlode = GeometricProblems.HenonHeilesPotential.lodeproblem([0.1,0.1],[0.1,0.1],timespan = (0,T),timestep = h_step)\n\nFor each dimension of generalized coordinates q = (q<sub>1</sub>, q<sub>2</sub>), we prescribe a expression that approximates the coordinates.\n\n@variables W1[1:4] W2[1:4] t\nq₁_expr = W1[1] *cos(W1[2]* t + W1[3]) + W1[4]\nq₂_expr = W2[1] *cos(W2[2]* t + W2[3]) + W2[4]\n\nthen define a basis object for variational Galerkin integrator, in this case, a Polynomial-Radial basis for the two dimension problem.\n\nPRB = PR_Basis{Float64}([q₁_expr,q₂_expr], [W1,W2], t,2)\n\nalong with a chosen quadrature rule, and initial guess for the parameters in the expression, we define the integrator, and start the game :)\n\nPR_Int = PR_Integrator(PRB, QGau8,[[0.14831,1.0,-0.64812,- 0.018712],[0.14298,- 0.97215,0.7615,-0.0013983]]) \nPR_sol = integrate(HHlode, PR_Int)\n\nOne good thing about continuous Galerkin variational integrators is that you could record the coordinates values between two discrete steps for free!\n\n\nHHlode_ref = GeometricProblems.HenonHeilesPotential.lodeproblem([0.1,0.1],[0.1,0.1],timespan = (0,TT),timestep = h_step/50)\nref_sol = integrate(HHlode_ref, ImplicitMidpoint())\n\nusing Plots\nsol_mat = hcat(PR_sol[2][:,2:end,1]'...)[1,:]\nplot(collect(h_step/40:h_step/40:TT),sol_mat,label = \"Continuous Solution\")\nscatter!(collect(0:h_step:TT),collect(PR_sol[1].q[:,1]),label = \"Discrete Solution\")\nplot!(collect(0:h_step/50:TT),collect(ref_sol[1].q[:,1]), label = \"Reference\")\n\n(Image: HenonHeilies)","title":"Basic Usage"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#Precision","page":"Precision","text":"Everything on the OGA path runs at the solver's working type T, with no implicit conversion.\n\nThis is not tidiness. A reduced-precision run that computes its seed in Float64 internally measures nothing about reduced precision — and OGA1dNormalEquations is exactly that failure mode, preserved deliberately as the baseline to measure against. Once the seed is a Float64 island, every statement of the form \"Float16 converges here\" is a statement about the island, not about Float16.","title":"Precision"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#The-rules","page":"Precision","text":"Every array on the path is an Array{T} — the atom matrix, the design matrix, the weights, the target, the residual, the coefficients, and every fit's internal factors. Built with zeros(T, …) / ones(T, …), never from untyped literals.\nNo bare float literals. Constants materialise through the argument or the type: one(T), zero(T), T(C) * eps(T), oftype(x, 0.044715), sqrt(eps(T)).\nReductions are spelled sqrt(sum(abs2, ·)), not norm — the generic norm fallback rescales through float, which is a promotion waiting to happen.\nsimpson_quadrature(nstages, T) is always passed T. Its signature defaults to Float64, and that default is precisely how the legacy path acquired its island.\nThe rank-revealing fits stay generic. PivotedQR and TruncatedSVD are hand-rolled so that they run at T; widening to Float32 to borrow LAPACK would reintroduce the island in miniature and is explicitly rejected.\nDiagnostics are quarantined. Condition numbers, smallest singular values and the study's comparable fit errors are computed in Float64 for reporting only, after the fit returns. Nothing derived from them may re-enter the fit.","title":"The rules"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#Three-documented-exceptions","page":"Precision","text":"Each computes a scalar or a grid coordinate in Float64 and converts once — never a matrix, and never a solve.\n\nNonlinearIntegrators.bias_grid and NonlinearIntegrators.weight_grid generate coordinates from an integer-indexed Float64 range and cast to T, so a large dict_amount cannot overflow the step to zero. (Computed at Float16, lo:(hi-lo)/n:hi throws ArgumentError: range step cannot be zero for n = 70000, since Float16(70000) == Inf.)\nThe regularization ladder is formed as T(2.0^k * sqrt(Float64(eps(T)))), because T(2)^k * sqrt(eps(T)) overflows to Inf for Float16 well inside the ladder.\nOGA1dNormalEquations, the baseline being measured against.","title":"Three documented exceptions"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#Enforcement","page":"Precision","text":"Two mechanisms, because prose in a docstring does not catch a regression.\n\nA runtime check on the activation. oga_fit calls NonlinearIntegrators.oga_check_precision once per fit and throws if σ(::T) is not a T:\n\njulia> oga_fit(OGA1d(), x -> max(0.0, x)^3, nodes, w, y, 4; bias_interval = [-π, π], dict_amount = 400)\nERROR: ArgumentError: activation returned Float64 for a Float16 argument, so the OGA seed\nwould not run at the working precision. Write the activation float-generically —\n`max(zero(x), x)^k`, `oftype(x, c)` — rather than with bare Float64 literals.\n\nmax(0.0, x)^k instead of max(zero(x), x)^k promotes every evaluation to Float64. It costs one scalar call per fit to rule out, and the failure it catches is otherwise visible only as suspiciously good half-precision accuracy — the worst kind of bug, because it looks like a result.\n\nNote the activation field of OneLayerNetwork_GML is untyped, so the activation is boxed as Any and a Float64-returning one would not be caught by inference. Hence a value-level check.\n\nValue-level assertions in the test suite. test/unit/oga_kernels.jl asserts eltype === T on every array oga_fit returns, for every dictionary × selection × fit combination, at Float16, Float32 and Float64 — with @inferred on top to catch the type instability that would let a promotion through. The existing assert_no_upcast helper covers the end-to-end path (eltype(q[end]) == T on the final state), and the studies record a run whose final state has left the working precision as its own upcast status rather than folding it into \"converged\".","title":"Enforcement"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#Float16:-squares-overflow-long-before-values-do","page":"Precision","text":"The half-precision ceiling is 65504, so a quantity of magnitude above about 256 cannot be squared, and two such quantities cannot be multiplied after squaring. That is easy to reach here: a ReLU³ atom over bias_interval = [-π, π] has norm ≈ 43, which squares to 1874, and two of those multiply to 3.5 million.\n\nTwo consequences shaped the implementation.\n\nIt broke the Jacobi SVD silently. The convergence test compared lvertbetarvert against varepsilon(T)sqrtalphagamma, a product of two squared column norms. Once it overflowed, the threshold became Inf, every column pair tested as \"already orthogonal\", and NonlinearIntegrators.jacobi_svd returned the unrotated matrix — wrong singular values, ‖UᵀU − I‖ = 1.27, and no error raised. The fix is to compare against sqrtalphasqrtgamma, which never forms the product.\n\nThe dictionary is rescaled. oga_fit multiplies the whole dictionary by a single power of two so the largest atom has norm ≈ 1, which keeps squared quantities near 1 instead of near lVertcdotrVert_max^2 throughout the factorisations. Three properties make this safe rather than a perturbation:\n\na power of two is exact in binary floating point — a pure exponent shift, no rounding — so Float64 and Float32 atom selection is bit-for-bit unchanged;\nevery row is scaled by the same factor, so even the non-scale-invariant RawProjection ranks candidates identically;\nthe residual needs no correction, since (sPsi)^top(cs) = Psi^topc; only the final coefficients are unscaled, by one multiplication.\n\nThe reciprocal itself must be representable: for a largest norm down near the subnormal range ldexp(one(Float16), 20) already overflows, so the scaling falls back to 1 rather than multiplying the dictionary by Inf.","title":"Float16: squares overflow long before values do"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#The-tolerances-have-to-scale-too","page":"Precision","text":"The same argument that motivates the sqrtvarepsilon(T) regularization ladder applies to the solver's residual tolerance, and getting it wrong invalidates a whole precision rather than degrading it.\n\nThe integrator default is f_textabstol = max(8 textttsolversize)varepsilon(textttdatatype(problem)) — scaled to the working precision, and merged with any options the caller passes. Both properties are required of the dependency ([compat] pins GeometricIntegratorsBase 0.5), because an absolute tolerance that does not scale with eps(T) is simply unreachable in reduced precision: at Float32 (varepsilon approx 12times10^-7) or Float16 (approx 98times10^-4) the run then sits at its residual floor and burns the entire iteration budget while parked on the right answer. Measured that way, ReLU³ at Float32 reports 1000 iterations at every regularization factor with an accuracy of 18times10^-7; read as non-convergence — which is what a naive status check does — that makes a whole precision column an artefact of the tolerance rather than a fact about the seed.\n\nThe studies pin an explicit f_textabstol = 256varepsilon(T) (oga_f_abstol in scripts/oga_activations.jl) on top of that, because the default scales with solversize, which varies with S across the sweep: pinning keeps cases of different network width comparable.","title":"The tolerances have to scale too"},{"category":"section","location":"Orthogonal Greedy Algorithm/precision/#Not-yet-device-ready","page":"Precision","text":"Precision genericity was the prerequisite for GPU portability — Float32 is the device-native type, and a Float64 island in the seed would have forced a host round-trip every time step — but the subsystem is not portable yet.\n\nWhat is already in the right shape: the dominant per-step operations are the selection scan and the coherence guard, each one mul! against the dictionary plus an argmax, i.e. a matrix–vector product and a reduction. Those map to a device directly. IncrementalQR also batches cleanly over the solution components, since each component's factorisation is independent.\n\nWhat blocks it: building the dictionary's design matrix and placing the selected neurons are still scalar loops, which would force scalar indexing on a device array. Getting there needs the dictionary build expressed as a broadcast or a kernel.\n\nOne trade-off to expect when it happens. The hand-rolled NonlinearIntegrators.pivoted_qr_lstsq and NonlinearIntegrators.jacobi_svd exist because LAPACK has no Float16 path; on a device at Float32 the vendor libraries are available instead and would be preferable — unpivoted QR through cuSOLVER's geqrf, and a Jacobi SVD through gesvdj. Note that cuSOLVER's pivoted QR support is thinner than its unpivoted support, so on a device IncrementalQR or TruncatedSVD is the more natural choice than PivotedQR. None of that changes the fit's cost, which is negligible either way (see the note at the top of the Algorithms page).","title":"Not yet device-ready"},{"category":"section","location":"Benchmarks/benchmarks/#Benchmarks","page":"Benchmarks","text":"The package ships a benchmark suite (under benchmark/) for the one-layer GML variational integrator NonLinear_OneLayer_GML. It runs each of several test problems through a large grid of integrator configurations and records, for every case, whether the nonlinear solve converged, how accurate the result is, how much the energy drifts, how many nonlinear iterations it took, and how long it ran. The results are written to CSV, summarised in a markdown report, and visualised with a set of plots.\n\nThe suite has three goals:\n\nfind which integrator-parameter configurations work well for each test problem;\nidentify issues in the package that are detrimental to performance;\nidentify robust solver strategies.\n\nThree further studies target the OGA initial guess specifically: scripts/oga_fit_study.jl measures seed quality on its own (no integrator, no Newton solve), scripts/oga_sweep.jl runs the end-to-end harmonic-oscillator sweep over seed variant × precision × regularization factor × activation, and scripts/oga_double_pendulum.jl repeats a reduced grid on the hardest problem. They live under scripts/ rather than here because they study one component rather than the integrator suite, and they are not part of the documentation build. See the Orthogonal Greedy Algorithm section and scripts/README.md.","title":"Benchmarks"},{"category":"section","location":"Benchmarks/benchmarks/#What-is-swept","page":"Benchmarks","text":"Each case integrates a problem for exactly 10 time steps; the time span is adapted per case as (0, 10·dt). The sweep spans, per problem:\n\naxis meaning\ntimestep dt integration step size\nprecision working floating-point type (Float16 / Float32 / Float64)\nR Gauss–Legendre quadrature order\nS number of hidden neurons (network width)\nactivation ReLUᵏ (k = 2, 3, 4), ELU, GELU, or tanh\nsolver strategy Newton with Static / Backtracking / StrongWolfe line search, or trust-region DogLeg\nλ Jacobian regularization (regularization_factor)\ninitial guess midpoint (IntegratorExtrapolation), Hermite (HermiteExtrapolation), or previous (NoExtrapolation)\n\nThe test problems (from GeometricProblems.jl) are the harmonic oscillator, the pendulum (a degenerate two-component IODE — it has no lodeproblem), the double pendulum, and the Toda lattice with N = 16.\n\nThe Toda lattice is currently excluded from the documentation build, though benchmark/run_toda_lattice.jl still runs it on request and full still includes it. Its network width has not been measured the way the other three now have (see Network width below), so its residual floors above the convergence target and every Float64 case runs its full iteration budget: about five hours for its quick grid, against seven minutes for the other three combined. It returns once a width has been chosen for it.","title":"What is swept"},{"category":"section","location":"Benchmarks/benchmarks/#Modes","page":"Benchmarks","text":"Each per-problem run file takes a mode — quick (default) or full — from its first command-line argument or from the GML_BENCH_PRESET environment variable.\n\naxis full quick\ndt 0.01, 0.1, 1.0, 10.0 0.1, 1.0, 10.0\nprecision Float16, Float32, Float64 Float64, Float32, Float16\nR 4, 8, 16 8 (16 for double pendulum)\nS 4, 6, 8 per problem: 10 harmonic, 8 pendulum, 10 double pendulum\nactivation ReLU², ReLU³, ReLU⁴, ELU, GELU, tanh GELU, tanh\nsolver Newton/{Static, Backtracking, StrongWolfe}, DogLeg DogLeg\nλ 0.0, 1e-7, 1e-5, 1e-3, 16√eps(T) 16√eps(T)\ninitial guess midpoint, Hermite, previous midpoint\nmax_iterations 10000 solver default (1000)\n\nquick is roughly 18 cases per problem (seconds to minutes each — the Toda lattice is the slowest because of its N = 16 state and larger network); full is on the order of tens of thousands of cases per problem (hours). Results are flushed to CSV per case, so an interrupted full run keeps its partial output.\n\nThe 16√eps(T) regularization scales the Jacobian-diagonal damping with the working precision: ≈2.4e-7 at Float64, ≈5.5e-3 at Float32, and 0.5 at Float16. The last is large and tends to over-damp half precision; note, however, that at half precision the ReLUᵏ basis is ill-conditioned and diverges independently of λ, whereas tanh still converges — the accuracy limit there is the precision, not the regularization.","title":"Modes"},{"category":"section","location":"Benchmarks/benchmarks/#Network-width","page":"Benchmarks","text":"S is set per problem rather than globally, because it decides the accuracy the ansatz can represent, and therefore whether the nonlinear solve has a target it can meet at all. A network too narrow for the trajectory floors its residual above the convergence tolerance; the solve then iterates to its cap without ever getting there, which is a maxiter however long it is given.\n\nMeasured at Float64/tanh/DogLeg over ten steps of dt = 0.1, ref_err against the sweep's own Gauss(8) reference:\n\nS harmonic oscillator pendulum double pendulum\n2 6.4e-05 3.5e-01 3.4e-04\n4 2.8e-06 8.0e-05 2.4e-05\n6 1.9e-07 1.4e-05 1.5e-06\n8 1.9e-11 2.9e-07 5.9e-08\n10 3.2e-14 5.8e-05 8.4e-10\n12 4.4e-13 1.4e+03 9.3e-10\n\nThree different shapes, which is why one global value will not do:\n\nThe harmonic oscillator improves by eight orders of magnitude from S = 4 to S = 10, and the iteration count collapses with it — 1000 at S = 4, 112 at S = 10, and nine at S = 12. Accuracy and cost improve together, because the solve stops chasing a target its ansatz cannot reach.\nThe pendulum has a sharp optimum at S = 8 and then diverges — 1.4e+03 at S = 12. Its ϑ is degenerate (p₂ = 0), which leaves the parameter Jacobian singular, and a wider network enlarges that null space. This is the one problem where over-parameterisation is the failure.\nThe double pendulum falls monotonically and then flattens: the gain from S = 10 to S = 12 is nothing, so S = 10 is where it stops.","title":"Network width"},{"category":"section","location":"Benchmarks/benchmarks/#The-half-precision-trade-off","page":"Benchmarks","text":"The widths above are chosen for Float64 accuracy, and that choice is paid for at Float16. It is a deliberate trade, not an oversight, so the size of it is recorded here.\n\nAs S grows, Float16 convergence falls while Float64 accuracy improves. Measured on the harmonic oscillator over the whole quick grid (36 cases per width, both solvers, three timesteps, two activations):\n\nS Float16 converged best Float64 ref_err\n4 17 / 36 2.8e-06\n8 12 / 36 1.9e-11\n10 9 / 36 3.4e-14\n\nA wider network is harder to condition in 11 bits of mantissa: more neurons means more nearly dependent columns in the parameter Jacobian, and half precision has no digits to spare in distinguishing them. So the same change that buys eight orders of magnitude at double precision costs roughly half the half-precision cases.\n\nThe widths were chosen this way because accuracy is what the suite exists to report and because Float16 is a robustness study rather than a production precision here — the Orthogonal Greedy Algorithm Initial Guess section covers half precision on its own terms, with the seed measured directly rather than through a solve. If half-precision robustness were the priority instead, S is the knob: S = 4 nearly doubles the Float16 success rate, at the cost of everything in the table above.","title":"The half-precision trade-off"},{"category":"section","location":"Benchmarks/benchmarks/#Metrics","page":"Benchmarks","text":"For each case the suite records:\n\nstatus — ok, or a failure class: maxiter (the nonlinear solve exhausted max_iterations — it still returns a finite state, so this is not convergence and is counted separately, following the same rule as the OGA studies in scripts/), singular, diverged, nonfinite, or the short name of any other exception raised;\nref_err — the relative max-norm error of the final state against a reference, which is a Gauss(8) integration at Float64 using the smallest timestep in the sweep (over the same 10-step horizon);\nham_drift — the maximum relative drift of the Hamiltonian over the run;\niterations — the nonlinear-solver iteration count of the final step;\nsolve_secs / total_secs — the summed nonlinear-solve time and the wall-clock time of the run.","title":"Metrics"},{"category":"section","location":"Benchmarks/benchmarks/#Running","page":"Benchmarks","text":"Instantiate the benchmark environment (it devs the package):\n\njulia --project=benchmark -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'\n\nRun one or more problems (mode defaults to quick):\n\njulia --project=benchmark benchmark/run_harmonic_oscillator.jl\njulia --project=benchmark benchmark/run_pendulum.jl\njulia --project=benchmark benchmark/run_double_pendulum.jl\njulia --project=benchmark benchmark/run_toda_lattice.jl full   # full sweep\n\nEach run writes benchmark/results/<problem>_<mode>.csv, a benchmark/results/<problem>_<mode>.md report, and PNG plots. Finally, aggregate every CSV present into a combined report:\n\njulia --project=benchmark benchmark/report.jl\n\nwhich writes benchmark/results/onelayer_gml_benchmark.md. The reporting step reads the CSVs, so a report can be regenerated (or restyled) without re-running the sweep. All benchmark/results/ contents are git-ignored.","title":"Running"},{"category":"section","location":"Benchmarks/benchmarks/#Outputs","page":"Benchmarks","text":"The CSV has one row per case with the columns\n\nproblem, T, dt, steps, R, S, activation, solver, linesearch, initial_guess,\nlambda, status, ref_err, ham_drift, iterations, solve_secs, total_secs\n\nThe markdown report contains a status breakdown, convergence/robustness tables (by solver strategy, initial-guess strategy, precision, and problem), the best configuration found per problem, and failure hot-spots. It embeds the plots:\n\nconvergence — success-rate bars per solver strategy, and a solver × precision heatmap (red = not converged, green = converged); the combined report also draws a success-rate bar per problem;\naccuracy, energy drift, run time, and nonlinear iterations — each as a scatter versus the timestep. A per-problem report colours the dots by precision; the combined report colours them by problem, so the four problems stay distinguishable.","title":"Outputs"},{"category":"section","location":"Benchmarks/benchmarks/#Results","page":"Benchmarks","text":"The figures on this page are not committed: they are regenerated at documentation-build time by a fresh quick run over the four problems (driven from docs/make.jl), so they track the current package. The narrative and tables are kept as an illustrative reference from one representative quick run — the numbers are not a fixed reference and may not match the freshly generated figures exactly. That run used the DogLeg solver, the midpoint initial guess, and the precision-scaled regularization λ = 16√eps(T).\n\nResults are organised as a summary across all problems followed by one section per problem (harmonic oscillator, pendulum, double pendulum, Toda lattice). The summary scatters are coloured by problem; each per-problem section shows that problem's scatters coloured by precision.","title":"Results"},{"category":"section","location":"Benchmarks/benchmarks/#Summary-across-all-problems","page":"Benchmarks","text":"Across the three problems (108 cases in the representative run), 53 met the solver's convergence criterion and a further 17 produced a finite trajectory without meeting it (maxiter). The two are counted separately, and the medians below are over all 70 runs that produced a trajectory, since a stalled run's accuracy is still measured.\n\nprecision cases converged success measured median ref_err median ham_drift median iter\nFloat16 36 9 25% 9 1.07e-02 2.14e-02 2\nFloat32 36 29 81% 29 4.88e-06 2.05e-05 6\nFloat64 36 15 42% 32 5.22e-07 2.38e-06 1000\n\nFloat32 is the most reliable column and Float64 the most accurate — a distinction worth keeping separate. Float64 is judged against f_abstol = max(8, solversize)·eps(T) ≈ 1.8e-15, which is a demanding target; where it is not met the run still integrates, and the median ref_err of 5.2e-07 against Float32's 4.9e-06 shows those runs are an order of magnitude more accurate than the ones that converged. The median iteration count of 1000 in that column is the flip side: reaching 1.8e-15 takes the whole budget when it is reachable at all. Float16 converges in a couple of iterations because its tolerance is 0.0078, and its accuracy is correspondingly the worst by four orders of magnitude. Success rate and accuracy are answering different questions.\n\nThe 25% in that Float16 row is also lower than it would be with a narrower network — see The half-precision trade-off for the measurement and why the widths were chosen that way regardless.\n\nSuccess rate broken down by problem, by solver strategy, and by solver × precision:\n\n(Image: Convergence success rate by problem)\n\n(Image: Convergence success rate by solver strategy)\n\n(Image: Convergence success rate by solver and precision)\n\nAccuracy, energy drift, run time and nonlinear-iteration counts versus the timestep, with all four problems overlaid and coloured by problem (each dot is one case that produced a trajectory, ok or maxiter). Accuracy and energy conservation degrade sharply as the timestep grows; at dt = 10 the 10-step horizon is far too coarse and the relative error is O(1).\n\n(Image: Accuracy versus timestep)\n\n(Image: Energy drift versus timestep)\n\n(Image: Run time versus timestep)\n\n(Image: Nonlinear iterations versus timestep)\n\nThe most accurate (lowest ref_err) configuration found for each problem, over the runs that produced a trajectory:\n\nproblem best ref_err T dt network solver iguess / λ\nharmonic_oscillator 3.42e-14 Float64 0.1 R8 S10 tanh DogLeg midpoint, λ=2.4e-7\npendulum 1.77e-08 Float64 0.1 R8 S8 gelu DogLeg midpoint, λ=2.4e-7\ndouble_pendulum 8.36e-10 Float64 0.1 R16 S10 tanh DogLeg midpoint, λ=2.4e-7\n\nAll three peak at Float64 and the smallest timestep, as expected. The quick preset sweeps only gelu and tanh; the ReLUᵏ powers are in full. Outright failures — as opposed to maxiter — concentrate at half precision and at the largest timestep dt = 10, consistent with the accuracy plot.","title":"Summary across all problems"},{"category":"section","location":"Benchmarks/benchmarks/#Harmonic-oscillator","page":"Benchmarks","text":"The simplest test problem: a single linear oscillator, and the most reliable of the three (27 of 36 converged in the representative quick run). With S = 10 it is also the most accurate, reaching ref_err = 3.4e-14 at dt = 0.1 in Float64 — and it gets there in ~100 iterations rather than exhausting the budget, because the ansatz can represent the trajectory to below the convergence tolerance. Each dot below is a case that produced a trajectory, coloured by precision.\n\n(Image: Accuracy versus timestep — harmonic oscillator)\n\n(Image: Energy drift versus timestep — harmonic oscillator)\n\n(Image: Run time versus timestep — harmonic oscillator)\n\n(Image: Nonlinear iterations versus timestep — harmonic oscillator)\n\n(Image: Convergence heatmap — harmonic oscillator)","title":"Harmonic oscillator"},{"category":"section","location":"Benchmarks/benchmarks/#Pendulum","page":"Benchmarks","text":"A degenerate two-component IODE (ϑ: p₁ = ml²q₂, p₂ = 0; it has no lodeproblem), included deliberately to stress the nonlinear solve — and the one problem whose accuracy gets worse with a wider network, since the degeneracy leaves the parameter Jacobian singular (see Network width). At its measured optimum S = 8 it reaches ref_err = 1.8e-08 at dt = 0.1, six orders better than the harmonic oscillator's floor but three worse than the oscillator's own optimum.\n\n(Image: Accuracy versus timestep — pendulum)\n\n(Image: Energy drift versus timestep — pendulum)\n\n(Image: Run time versus timestep — pendulum)\n\n(Image: Nonlinear iterations versus timestep — pendulum)\n\n(Image: Convergence heatmap — pendulum)","title":"Pendulum"},{"category":"section","location":"Benchmarks/benchmarks/#Double-pendulum","page":"Benchmarks","text":"A four-dimensional chaotic system, and the hardest of the three: it accounts for every singular case in the representative run and has the lowest convergence rate (9 of 36). Quick mode uses a larger quadrature order and network (R = 16, S = 10) than the two simple problems; tanh gives the best accuracy here, ref_err = 8.4e-10 at dt = 0.1 in Float64.\n\n(Image: Accuracy versus timestep — double pendulum)\n\n(Image: Energy drift versus timestep — double pendulum)\n\n(Image: Run time versus timestep — double pendulum)\n\n(Image: Nonlinear iterations versus timestep — double pendulum)\n\n(Image: Convergence heatmap — double pendulum)","title":"Double pendulum"},{"category":"section","location":"Benchmarks/benchmarks/#Toda-lattice-(N-16)","page":"Benchmarks","text":"The largest problem, with a 16-dimensional state and a correspondingly larger network. It is not part of the documentation build, so no figures are generated for it here.\n\nIts network width has not been measured the way the other three have, and the consequence is not merely a slower run: with a width too narrow for the trajectory, the residual floors above the convergence tolerance and every Float64 case exhausts its iteration budget. That puts its quick grid at roughly five hours against seven minutes for the other three combined, which is why it is excluded rather than capped — an iteration cap would hide the cause instead of fixing it.\n\nTo run it:\n\njulia --project=benchmark benchmark/run_toda_lattice.jl quick\n\nIt is also included in full. Once a width has been chosen for it — the same measurement as in Network width above — it returns to this page.","title":"Toda lattice (N = 16)"},{"category":"section","location":"Benchmarks/benchmarks/#Extending","page":"Benchmarks","text":"To add a problem, copy one of the run_*.jl files and supply a build_prob(T, timespan, timestep) closure returning an AbstractProblemIODE at element type T, plus a hamiltonian(t, q, p, params) closure; then call run_sweep. Per-problem axis overrides (as the double pendulum and Toda lattice use for R and S) are passed as the Rs / Ss keyword arguments to run_sweep. The shared engine, presets, and reporting live in benchmark/gml_benchmark_common.jl and benchmark/gml_report.jl.","title":"Extending"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#Orthogonal-Greedy-Algorithm-Initial-Guess","page":"Overview","text":"The network integrators in this package (NonLinear_OneLayer_GML, Hardcode_int, Time_reversible_OneLayer, Time_reversible_Hardcode_int) solve, at every time step, a nonlinear system for the parameters of a shallow neural network that represents the trajectory between two discrete nodes. That nonlinear (Newton) solve needs a good initial guess. This guess is produced by an Orthogonal Greedy Algorithm (OGA) [1], a form of greedy training for shallow networks [2]: candidate neurons are drawn from a fixed dictionary, the neuron most correlated with the current residual is added one at a time, and the output weights are refit by (quadrature-weighted) least squares after each addition.\n\nAll of it lives in src/oga/, behind a single composable type.","title":"Orthogonal Greedy Algorithm Initial Guess"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#The-pages","page":"Overview","text":"Page Contents\nTheory what is being approximated, why the classical dictionary has ±1 weights, the greedy selection criterion and where it comes from, the conditioning analysis\nAlgorithms every dictionary, selection rule and fit in turn — mechanism, implementation, cost, when to use\nUsage presets, composing your own, per-integrator behaviour, reading the result, extending\nPrecision the no-implicit-conversion invariant, how it is enforced, and the Float16 traps\nStudies the two-tier measurement setup and what it found","title":"The pages"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#The-shape-of-the-problem","page":"Overview","text":"Per time step and per solution component, the algorithm greedily builds\n\nu(t) = sum_k=1^S c_k sigma(w_k t + b_k)\n\nby repeatedly picking, from a fixed dictionary of candidate neurons (w, b), the atom most correlated with the current fit residual, then refitting all output weights c by a quadrature-weighted least-squares solve. That refit — the orthogonal part, as opposed to plain matching pursuit, which would fit only the new atom — is where all the numerical difficulty lives.\n\nThe seed is configured along three independent axes, crossed as fields of one OGA type rather than enumerated as a type per combination:\n\ndictionary (OGADictionary) — which candidate neurons are on offer;\nselection (OGASelection) — how candidates are ranked against the residual;\nfit (OGAFit) — how the output weights are refit.\n\nNamed presets recover the useful corners: OGA1d (the default, and the default), OGA1dNormalized, OGA1dStable, OGA2d, OGASphere. The original-paper reference implementation is kept separately as OGA1dNormalEquations.","title":"The shape of the problem"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#Why-this-needed-rework","page":"Overview","text":"Two problems motivated the current design.","title":"Why this needed rework"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#1.-The-seed-collapsed-at-reduced-precision","page":"Overview","text":"The original fit solved the least-squares problem through the normal equations, forming the Gram matrix G = Phioperatornamediag(w)Phi^top. That squares the condition number, kappa(G) = kappa(Phi)^2 [3], [4], which halves the number of usable digits — and the dictionary is coherent enough that the squared condition number exceeded 1varepsilon at Float32 while still fitting under it at Float64. Hence the Float64 island: the seed was assembled in double precision regardless of the solver's working type.\n\nCrucially the island buys no accuracy. The OGA result is a seed, rounded back to T the moment it is stored, and the final accuracy is set by the working-precision Newton solve. It buys only robustness of an ill-conditioned solve, which a well-conditioned formulation does not need.\n\nThree hard-coded guard-rail constants encoded the same Float64 assumption and were silently ineffective in reduced precision:\n\na dictionary-norm floor dict_norms < 1e-12, which sits below varepsilon(textttFloat32) approx 12times10^-7 and so never fired;\nTikhonov ridges G + 1e-12·I and G + 1e-14·I, which round away entirely below varepsilon(textttFloat32);\na bias grid lo:(hi-lo)/dict_amount:hi that threw ArgumentError: range step cannot be zero at Float16, because a large dict_amount overflows Float16(dict_amount) to Inf.\n\nThe fit is now a QR factorisation of the sqrtw-scaled design matrix (NonlinearIntegrators.weighted_lstsq), conditioned on kappa(Phi) rather than kappa(Phi)^2, and every guard rail scales with varepsilon(T). See Theory for the analysis and Algorithms for the alternatives — rank-revealing and incremental factorisations, and a selection rule that makes a rank-deficient selected set impossible rather than merely detectable.","title":"1. The seed collapsed at reduced precision"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#2.-The-dictionary-was-ReLU-only","page":"Overview","text":"Both original variants used {±1} crossed with a bias grid. That set is complete for a positively homogeneous activation — for ReLUᵏ, the magnitude of w carries no shape information that the bias grid and the output weight do not already absorb, so only its sign remains. It starves ELU, GELU and tanh, for which w sets a genuine transition length scale. WeightBiasGrid2d and AngularGrid supply that missing degree of freedom; restricting the former to {±1} recovers the original dictionary exactly, so it is a strict generalisation. See Theory.","title":"2. The dictionary was ReLU-only"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#A-didactic-Float16-example","page":"Overview","text":"The following self-contained example reproduces, in miniature, the failure that forces a Float64 island on the normal-equations formulation. We build four ReLU³ neurons — two of them with almost identical biases (0.300 and 0.305), the situation that makes the Gram matrix ill-conditioned — and fit a target that is a known combination of them. We solve the least-squares problem both ways — normal equations / Gram matrix, and QR on the sqrtw-scaled design matrix — in Float64 and in Float16.\n\nusing LinearAlgebra\n\nσ(x) = max(zero(x), x)^3          # ReLU³ activation\n\nfunction setup(::Type{T}) where {T}\n    t = T.(range(0, 1; length = 9))                 # quadrature nodes on [0,1]\n    w = fill(one(T) / 9, 9)                         # (toy) positive weights\n    biases = T[0.300, 0.305, -0.20, 1.50]           # atoms 1 and 2 nearly identical\n    Φ = reduce(vcat, (permutedims(σ.(t .+ b)) for b in biases))   # (natoms × nnodes)\n    y = T(0.7) .* σ.(t .+ T(0.30)) .- T(0.4) .* σ.(t .- T(0.20))  # target\n    return t, w, Φ, y\nend\n\n# OLD: normal-equations (Gram) solve\nfunction gram_solve(Φ, w, y)\n    G   = Φ * (w .* Φ')\n    rhs = Φ * (w .* y)\n    return G, G \\ rhs\nend\n\n# NEW: QR on the √w-scaled design matrix\nfunction qr_solve(Φ, w, y)\n    sw = sqrt.(w)\n    Â  = sw .* Φ'\n    return Â, Â \\ (sw .* y)\nend\nnothing # hide\n\nIn Float64 both methods recover the true weights [0.7, 0, -0.4, 0], and we can see the condition number relationship kappa(G) = kappa(hatA)^2 directly:\n\nt, w, Φ, y = setup(Float64)\nG, xg = gram_solve(Φ, w, y)\nÂ, xq = qr_solve(Φ, w, y)\n\nfit_err(x) = sqrt(sum(w .* (Φ' * x .- y) .^ 2))\n\nprintln(\"cond(G)  = \", cond(G))\nprintln(\"cond(Â)² = \", cond(Â)^2)\nprintln(\"Gram x   = \", xg, \"   fit-err = \", fit_err(xg))\nprintln(\"QR   x   = \", xq, \"   fit-err = \", fit_err(xq))\n\nIn Float16, the achievable condition number is only about 1texttteps(Float16) approx 10^3. The Gram condition number is far beyond that, so the Gram solve returns finite garbage — the two near-duplicate atoms receive huge weights of opposite sign — while the QR solve stays bounded and close to the truth:\n\nt, w, Φ, y = setup(Float16)\nG, xg = gram_solve(Φ, w, y)\nÂ, xq = qr_solve(Φ, w, y)\n\nfit_err(x) = sqrt(sum(w .* (Φ' * x .- y) .^ 2))\n\nprintln(\"1/eps(Float16) = \", 1 / eps(Float16))\nprintln(\"cond(G)        = \", Float64(cond(Float64.(G))))\nprintln(\"cond(Â)        = \", Float64(cond(Float64.(Â))))\nprintln(\"Gram x = \", Float64.(xg), \"   fit-err = \", Float64(fit_err(xg)))\nprintln(\"QR   x = \", Float64.(xq), \"   fit-err = \", Float64(fit_err(xq)))\n\nThe QR result matches what the shipped fit produces (weighted_lstsq implements exactly this QR solve, plus the ridged fallback for the rank-deficient case):\n\nusing NonlinearIntegrators\nt, w, Φ, y = setup(Float16)\nprintln(\"weighted_lstsq x = \", Float64.(NonlinearIntegrators.weighted_lstsq(Φ, w, y)))\n\nIn the full integrator the Float16 Gram garbage does not merely produce a poor seed: fed into the Newton solve it makes the parameter Jacobian singular (two nearly identical neurons are nearly linearly dependent), surfacing as a SingularException or a NaN. The QR reformulation, the coherence guard and the ridged fallback together keep the seed finite and well-behaved, so the run proceeds at the working precision — and the Studies measure how far that gets each variant.","title":"A didactic Float16 example"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#API","page":"Overview","text":"The helpers — weighted_lstsq, scaled_lstsq, ridged_lstsq, oga_norm_floor, oga_tikhonov, bias_grid, weight_grid, pivoted_qr_lstsq, jacobi_svd, truncated_svd_lstsq, oga_check_precision and the IncrementalQRState factorisation — are documented with their full docstrings in the API listing on the home page.","title":"API"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#References","page":"Overview","text":"V. N. Temlyakov. Greedy approximation. Acta Numerica 17, 235–409 (2008).\n\n\n\nJ. W. Siegel, Q. Hong, X. Jin, W. Hao and J. Xu. Greedy training algorithms for neural networks and applications to PDEs. Journal of Computational Physics 484, 112084 (2023).\n\n\n\nG. H. Golub and C. F. Van Loan. Matrix Computations. 4th Edition (Johns Hopkins University Press, Baltimore, 2013).\n\n\n\nN. J. Higham. Accuracy and Stability of Numerical Algorithms. 2nd Edition (Society for Industrial and Applied Mathematics, 2002).\n\n\n\n","title":"References"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#Theory","page":"Theory","text":"This page sets out what the greedy algorithm is approximating, why the classical dictionary has the form it does, what distinguishes orthogonal greedy from plain matching pursuit, and where the numerical difficulty comes from. The Algorithms page then treats each implemented component in turn.","title":"Theory"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#The-approximation-problem","page":"Theory","text":"Over one time step the integrator represents each component of the trajectory by a shallow network with S hidden units,\n\nu(t) = sum_k=1^S c_k sigmaleft(w_k t + b_kright) \nqquad t in 0 1 \n\nwith the step mapped affinely onto the unit interval. The unknowns are the output weights c in mathbbR^S and the hidden parameters (w_k b_k). The variational integrator determines them by a nonlinear system; the OGA's job is only to produce a starting point for that system, by fitting u to a target y sampled at the network's input nodes.\n\nThe fit is measured in a quadrature-weighted L^2 norm. With nodes t_j and positive quadrature weights w_j (Simpson's rule on the network's input grid),\n\nlangle f g rangle_w = sum_j w_j f(t_j) g(t_j) \nqquad lVert f rVert_w^2 = langle f f rangle_w \n\nwhich is the discrete stand-in for int_0^1 f g mathrmdt. Writing Phi_ij = sigma(w_i t_j + b_i) for the value of dictionary atom i at node j, the fit for a fixed set of hidden parameters is the linear least-squares problem\n\nmin_c  sum_j w_j left( sum_i c_i Phi_ij - y_j right)^2 \ntagLS","title":"The approximation problem"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#Greedy-approximation","page":"Theory","text":"The hidden parameters enter nonlinearly, so the fit over all of them at once is a non-convex problem — which is precisely the problem the integrator's Newton solve is already struggling with. Greedy approximation [1] sidesteps it: fix a countable dictionary mathcalD = g_a of candidate atoms, and build the approximation one atom at a time, each time choosing the atom that best reduces the current residual. For shallow networks this is a form of greedy training [2].\n\nTwo variants differ in what happens to the coefficients already chosen:\n\nMatching pursuit fits only the newly added atom, leaving earlier coefficients untouched.\nOrthogonal greedy re-solves (LS) over all selected atoms after every addition.\n\nThis package implements the orthogonal variant — hence the name — and the refit is where all the numerical difficulty lives.\n\nThe algorithm, with Lambda_k the index set selected after k steps:\n\nbeginaligned\nr_0 = y quad Lambda_0 = emptyset \ntextbffor  k = 1 dots S \nqquad a_k = argmax_a mathrmscore(g_a r_k-1 Lambda_k-1)\n   text(selection) \nqquad Lambda_k = Lambda_k-1 cup a_k \nqquad c^(k) = argmin_c biglVert textstylesum_i in Lambda_k c_i g_i - y bigrVert_w^2\n   text(refit) \nqquad r_k = y - textstylesum_i in Lambda_k c^(k)_i g_i\n   text(residual)\nendaligned\n\nThe two lines marked selection and refit are exactly the two axes the implementation exposes as configurable (OGASelection and OGAFit); the dictionary mathcalD is the third (OGADictionary).","title":"Greedy approximation"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#Why-1-weights-are-the-classical-dictionary","page":"Theory","text":"The dictionary both original variants used is {±1} crossed with a uniform grid of biases. That is not an arbitrary discretisation — for the activation the method was derived for, it is complete.\n\nReLUᵏ, sigma(x) = max(0 x)^k, is positively homogeneous of degree k: sigma(lambda x) = lambda^k sigma(x) for lambda  0. Hence for w neq 0\n\nsigma(w t + b)\n  = sigmaleft(lvert wrvert leftoperatornamesign(w) t + tfracblvert wrvertrightright)\n  = lvert wrvert^k sigmaleft(operatornamesign(w) t + tfracblvert wrvertright) \ntagH\n\nRead (H) as a statement about what w can and cannot change. Its magnitude produces only two effects: a scalar factor lvert w rvert^k, which the output weight c_k absorbs, and a rescaling of the bias, which the bias grid already covers. Neither changes the atom's shape. What survives is the sign of w, which genuinely does — for k geq 1, sigma(t + b) and sigma(-t + b) are different functions (one increasing, one decreasing on their active region), and no rescaling relates them.\n\nSo for ReLUᵏ the set\n\nmathcalD_1 = pm 1 times b_0 dots b_n\n\ncovers every ridge direction the greedy step could want, and enlarging it with more weight magnitudes adds nothing but duplicates. This is the shallow-ReLU approximation-theory setting, and it is why BiasGrid1d is the default.","title":"Why ±1 weights are the classical dictionary"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#Where-the-argument-fails","page":"Theory","text":"Neither ELU nor GELU is positively homogeneous, so (H) does not hold for them and the conclusion collapses. For a smooth, saturating activation, w controls the length scale of the transition — how sharply the unit switches — and that is a genuine shape parameter, independent of both b (which sets where the transition happens) and c (which sets its amplitude). Consider tanh(wt + b): increasing lvert wrvert sharpens the step, and no combination of a bias shift and an output weight reproduces it.\n\nWith lvert w rvert pinned to 1, the dictionary can therefore only offer transitions of a single steepness, positioned along the bias axis. If the target needs a sharper or gentler one, the greedy step cannot supply it; it compensates by stacking several atoms that partially cancel, which is both a poor fit and a badly conditioned one. This is the mechanism behind the measured regression of GELU under the ReLU-theory seed, and the motivation for WeightBiasGrid2d and AngularGrid.","title":"Where the argument fails"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#The-atom-sphere","page":"Theory","text":"There is a cleaner way to see both cases at once. Write the atom's parameters as a vector (w b) in mathbbR^2 and split it into a radius and a direction. By (H), for a homogeneous activation the radius only rescales the atom, so the shape depends on the direction alone — a point on the unit circle. The natural dictionary is therefore a grid on that circle, which is how greedy approximation theory states it for mathbbR^d+1: a grid on the unit sphere.\n\nThat is what AngularGrid builds. It also explains a weakness of the bias grid: uniform spacing in b at fixed lvert w rvert = 1 is not uniform on the circle. It concentrates resolution at large lvert b rvert, where the atom is nearly constant on 01 and carries little information, and spreads it thinly near the origin, where the interesting transitions live. For a non-homogeneous activation the radius stops being redundant, and log-spaced radii recover the length-scale freedom without having to choose a weight interval.","title":"The atom sphere"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#The-selection-criterion","page":"Theory","text":"What should score be? Take the residual r orthogonal to the span of the atoms already selected — which it is, by construction, since the refit is a projection. Adding a candidate g and refitting reduces the squared residual by exactly\n\nlVert r rVert_w^2 - lVert r_textnew rVert_w^2\n  = fraclangle r g^perp rangle_w^2lVert g^perp rVert_w^2\n  = fraclangle r g rangle_w^2lVert g^perp rVert_w^2 \ntagG\n\nwhere g^perp is the component of g orthogonal to the selected span. The second equality uses r perp operatornamespan, which kills the parallel part of g in the inner product but not in the norm.\n\nSo the greedy step that maximises the one-step residual reduction ranks candidates by\n\nmathrmscore(g) = fraclvertlangle r grangle_wrvertlVert g^perprVert_w \n\nwhich is OrthogonalProjection. Two simplifications of it are also implemented:\n\nreplacing lVert g^perp rVert_w by lVert g rVert_w, which ignores overlap with the selected set but keeps scale invariance — NormalizedProjection. It is exact at the first step, where the selected set is empty and g^perp = g.\ndropping the denominator altogether — RawProjection, the default rule. This is not scale invariant: a large-norm atom outranks a better-aligned small one. Harmless when all atoms have comparable norms, as on the ±1 grid; wrong on a 2-D grid, where norms differ by orders of magnitude.\n\nTwo caveats worth stating plainly. First, (G) is a one-step result: maximising it at every step does not maximise the reduction after S steps. Greedy myopia is real here and shows up in the measurements — on the harmonic-oscillator label sets, RawProjection selects a worse first atom than the optimum but a better quartet. Second, (G) is about the fit residual, whereas what actually matters is how well the seed starts the Newton solve. The two are correlated but not identical, which is why the Studies are split into a seed tier and an end-to-end tier.","title":"The selection criterion"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#The-rank-gain-floor","page":"Theory","text":"(G) also gives a rank criterion for free. lVert g^perprVert_w is precisely the amount of genuinely new direction the atom contributes: it is the diagonal entry the atom would produce in a QR factorisation of the selected design matrix. If it collapses, the atom is (numerically) in the span of its predecessors, and admitting it makes the selected set rank-deficient.\n\nOrthogonalProjection therefore refuses any atom with\n\nlVert g^perp rVert_w  textttmin_gain cdot lVert g rVert_w \nqquad textttmin_gain = sqrtvarepsilon(T)  textby default\n\na relative test, so it is scale invariant. This is the direct fix for the reduced-precision failure: a SingularException: zero pivot found at index 3 — out of only four neurons — is exactly this quantity going to zero at the third selected atom, and the guard makes selecting such an atom impossible rather than merely detecting it afterwards.","title":"The rank-gain floor"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#Conditioning:-why-the-fit-needed-care","page":"Theory","text":"Fold the positive quadrature weights into a row scaling, hatA = operatornamediag(sqrtw)Phi^top and haty = operatornamediag(sqrtw)y. Then (LS) becomes an ordinary least-squares problem min_c lVert hatA c - hatyrVert_2, and the weighting disappears from every subsequent formula. The implementation works entirely in this scaled space.\n\nThe original code solved (LS) through the normal equations\n\nGc = Phi operatornamediag(w) y  qquad\nG = Phi operatornamediag(w) Phi^top = hatA^tophatA \n\nForming G squares the condition number, kappa(G) = kappa(hatA)^2 [3], [4]. Since a linear system is solvable in floating point only while kappa lesssim 1varepsilon(T), squaring kappa halves the number of usable digits. That is the whole story of the Float64 island: the dictionary is coherent enough that kappa(hatA) is already large — near-duplicate biases give near-parallel columns [5] — so kappa(hat A)^2 exceeded 1varepsilon at Float32 while still fitting under it at Float64.\n\nSolving via a QR factorisation of hatA instead [6] never forms G, so the accuracy is governed by kappa(hatA) — recovering roughly the factor of two in digits, which is exactly the Float64→Float32 gap. This is NonlinearIntegrators.weighted_lstsq and the WeightedQR fit.\n\nNote what the island does not buy. The OGA result is a seed: it is rounded back to T the moment it is stored, and the final accuracy is set by the working-precision Newton solve. Double precision buys only robustness of an ill-conditioned solve, which a well-conditioned formulation does not need.","title":"Conditioning: why the fit needed care"},{"category":"section","location":"Orthogonal Greedy Algorithm/theory/#References","page":"Theory","text":"N. J. Higham and T. Mary. Mixed precision algorithms in numerical linear algebra. Acta Numerica 31, 347–414 (2022).\n\n\n\nÅ. Björck. Numerical Methods for Least Squares Problems (Society for Industrial and Applied Mathematics, 1996).\n\n\n\n","title":"References"},{"category":"section","location":"#NonlinearIntegrators","page":"Home","text":"<!– Julia Package aims to generalize Galerkin variational integrator with polynomial basis to nonlinear basis. –>","title":"NonlinearIntegrators"},{"category":"type","location":"#NonlinearIntegrators.AngularGrid","page":"Home","text":"AngularGrid(; radii = (1.0,), amount = nothing)\n\nAtoms placed on rays through the origin of (w, b) space: (w, b) = r · (cos θ, sin θ) for a uniform grid of amount + 1 angles over [0, 2π) and each radius in radii. amount = nothing uses the method's dict_amount.\n\nThis is the dictionary the underlying approximation theory is stated for — a grid on the unit sphere of ℝ^{d+1} — and it unifies the 1-D and 2-D cases. For a homogeneous activation the radius is redundant (it only rescales the atom), so a single radius suffices and the set covers the same ridge directions as {±1} × (bias grid), but sampled uniformly in atom space rather than uniformly in bias: the ±1 grid over [-π, π] clusters its resolution where |b| is large and the atom is nearly constant on t ∈ [0, 1], which is where it matters least. For a smooth activation the radius is the length scale, and log-spaced radii give the second degree of freedom without having to pick a weight interval.\n\nThe full circle is used rather than a half circle because ReLUᵏ(x) and ReLUᵏ(-x) are different functions — the sign of w is real shape information (only the scale is redundant).\n\n\n\n\n\n","title":"NonlinearIntegrators.AngularGrid"},{"category":"type","location":"#NonlinearIntegrators.BiasGrid1d","page":"Home","text":"BiasGrid1d()\n\nThe original dictionary: weights w = ±1 crossed with a uniform grid of dict_amount + 1 biases over the method's bias_interval, for 2·(dict_amount + 1) atoms.\n\nComplete for ReLUᵏ (see the discussion at the top of this file) and the default. Atom order — the w = -1 block first, then w = +1 — is load-bearing: argmax breaks ties by first index, so reordering changes which atoms are selected.\n\n\n\n\n\n","title":"NonlinearIntegrators.BiasGrid1d"},{"category":"type","location":"#NonlinearIntegrators.CGVI_standard","page":"Home","text":"Continuous Galerkin Variational Integrator.\n\nb: weights of the quadrature rule\nc: nodes of the quadrature rule\nx: nodes of the basis\nm: mass matrix\na: derivative matrix\nr₀: reconstruction coefficients at the beginning of the interval\nr₁: reconstruction coefficients at the end of the interval\n\n\n\n\n\n","title":"NonlinearIntegrators.CGVI_standard"},{"category":"type","location":"#NonlinearIntegrators.IncrementalQR","page":"Home","text":"IncrementalQR()\n\nReuse the incrementally maintained QR factorisation (see IncrementalQRState): one triangular solve per greedy step instead of a fresh k × k factorisation.\n\nNumerically equivalent to WeightedQR up to rounding, but O(k · nnodes) rather than O(k² · nnodes) per step, and it is the fit that shares its Q with OrthogonalProjection — pairing the two is the \"textbook\" efficient and stable OGA.\n\n\n\n\n\n","title":"NonlinearIntegrators.IncrementalQR"},{"category":"type","location":"#NonlinearIntegrators.IncrementalQRState","page":"Home","text":"IncrementalQRState{T}(nnodes, maxcols)\n\nThin QR factorisation of the greedily selected design matrix, maintained by appending one column at a time.\n\nOGA adds exactly one atom per iteration, so re-solving a k × k system from scratch at every step repeats work the previous step already did. Maintaining Q (orthonormal columns) and R (upper triangular) instead costs O(k · nnodes) per step, never forms a Gram matrix, and, as a side effect, yields two quantities the greedy loop wants anyway:\n\nthe norm of the new column after deflation against the already-selected ones, which is the rank-gain the atom actually contributes (see OrthogonalProjection); and\nQ itself, which turns the orthogonal-greedy selection score into one matrix product against the dictionary.\n\nColumns are stored √w-scaled, so the Euclidean inner products here are the quadrature-weighted ones and no re-scaling happens inside the loop.\n\n\n\n\n\n","title":"NonlinearIntegrators.IncrementalQRState"},{"category":"type","location":"#NonlinearIntegrators.MirrorPairs","page":"Home","text":"MirrorPairs()\n\nOne atom (w, b) → two neurons (w, b) and (-w, w + b) → two independent design columns, so each member of the pair gets its own output weight.\n\nThe map (w, b) ↦ (-w, w + b) sends σ(w t + b) to σ(w (1 - t) + b), i.e. reflects the neuron about the midpoint of the unit time interval. Used by Time_reversible_OneLayer.\n\nSince neurons come in pairs, the requested neuron count must be even; oga_fit rejects an odd one rather than quietly placing one neuron fewer.\n\n\n\n\n\n","title":"NonlinearIntegrators.MirrorPairs"},{"category":"type","location":"#NonlinearIntegrators.NoSymmetry","page":"Home","text":"NoSymmetry()\n\nOne atom → one neuron → one design-matrix column.\n\n\n\n\n\n","title":"NonlinearIntegrators.NoSymmetry"},{"category":"type","location":"#NonlinearIntegrators.NormalEquationsFit","page":"Home","text":"NormalEquationsFit(; ridge = true, island = false)\n\nSolve the normal equations G x = Φ diag(w) y with G = Φ diag(w) Φᵀ, optionally with the precision-scaled Tikhonov ridge of oga_tikhonov (ridge = true) and optionally in Float64 regardless of the working precision (island = true).\n\nThis is the baseline, not a recommendation: forming G squares the condition number. It exists so that \"island vs. working precision\" and \"ridge vs. no ridge\" are two knobs on one code path that can be ablated in a benchmark, rather than differences buried in forked implementations. NormalEquationsFit(ridge = false, island = true) reproduces the arithmetic of OGA1dNormalEquations, the original-paper reference.\n\nNote island = true deliberately violates the package's precision discipline; it is the thing being measured against.\n\n\n\n\n\n","title":"NonlinearIntegrators.NormalEquationsFit"},{"category":"type","location":"#NonlinearIntegrators.NormalizedProjection","page":"Home","text":"NormalizedProjection()\n\nScore by the cosine-like ratio |⟨r, g⟩_w| / ‖g‖_w.\n\nThis is the textbook greedy criterion: it measures how much of the residual the atom explains, independently of the atom's amplitude, which the output weight absorbs anyway. Scale invariance makes it mandatory for WeightBiasGrid2d and AngularGrid, where atoms differ in norm by orders of magnitude.\n\n\n\n\n\n","title":"NonlinearIntegrators.NormalizedProjection"},{"category":"type","location":"#NonlinearIntegrators.OGA","page":"Home","text":"OGA(dictionary = BiasGrid1d(), selection = RawProjection(), fit = WeightedQR();\n    coherence = true, norm_guard = true, fill_unused = true)\n\nOrthogonal Greedy Algorithm initial guess for the network integrators.\n\nAt every time step the integrator must solve a nonlinear system for the parameters of a shallow network u(x) = Σₖ cₖ σ(wₖ x + bₖ). The OGA produces the starting point: it repeatedly picks, from a fixed dictionary of candidate neurons (w, b), the atom most correlated with the current fit residual, then refits all output weights c by a quadrature-weighted least-squares solve — the \"orthogonal\" part, as opposed to plain matching pursuit, which would only fit the new atom.\n\nThe three axes:\n\ndictionary::OGADictionary — the candidate neuron set: the original {±1} × (bias grid) (BiasGrid1d), a genuine 2-D (w, b) grid (WeightBiasGrid2d), an angular grid on the atom sphere (AngularGrid), or any of them with off-grid polish (Refined).\nselection::OGASelection — how candidates are ranked: RawProjection, NormalizedProjection or OrthogonalProjection.\nfit::OGAFit — how the output weights are refit: WeightedQR, IncrementalQR, PivotedQR, TruncatedSVD or NormalEquationsFit.\n\nGuard rails, all scaled to eps(T) rather than to absolute constants:\n\ncoherence — after an atom is selected, block dictionary atoms whose weighted L² coherence with it exceeds 1 - sqrt(eps(T)). Inert at Float64/Float32; it only bites where distinct atoms have rounded together.\nnorm_guard — treat atoms whose weighted norm falls below oga_norm_floor as unusable rather than normalising by noise. Atoms with a non-finite norm (reachable at Float16 with a high ReLUᵏ power over a wide bias interval, where σ(b)ᵏ overflows) are always excluded, guard or not.\nfill_unused — when the greedy loop runs out of usable atoms before all S neurons are placed, give the remaining neurons distinct, well-separated (w, b) with zero output weight. Without this they would all keep (0, 0) and become identical rows of the Newton Jacobian — trading a rank-deficient seed for a rank-deficient solve.\n\nEverything runs at the solver's working precision T; see the precision note in src/oga/numerics.jl.\n\nExamples\n\nNonLinear_OneLayer_GML(basis, quadrature)                                # OGA1d(), the default\nNonLinear_OneLayer_GML(basis, quadrature; initial_guess_method = OGA2d())\nNonLinear_OneLayer_GML(basis, quadrature;\n    initial_guess_method = OGA(BiasGrid1d(), OrthogonalProjection(), TruncatedSVD()))\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA"},{"category":"type","location":"#NonlinearIntegrators.OGA1dNormalEquations","page":"Home","text":"OGA1dNormalEquations()\n\nThe reference implementation from the original paper, kept as a selectable baseline for comparison.\n\nThe dictionary and the greedy least-squares fit are assembled in Float64 — a \"double-precision island\" — the output weights come from the normal equations Gₖ xₖ = bₖ, and the result is rounded into the working-precision cache. It carries none of the precision-scaled guard rails: no norm floor, no coherence guard, no ridge, and no rank detection.\n\nThat combination is why it is the baseline rather than the default. Forming Gₖ squares the condition number, so the fit needs roughly twice the digits the problem does, which is what the Float64 island supplies; and at 16 bits the atom selection degrades until the third or fourth selected neuron is linearly dependent on its predecessors, at which point the Gram solve raises SingularException before the Newton iteration has begun. See the \"Orthogonal Greedy Algorithm\" section of the documentation for the full analysis, and NormalEquationsFit for the same arithmetic available as a fit strategy inside the modern composable OGA, where the island and the ridge can be toggled independently.\n\nSelect it with NonLinear_OneLayer_GML(...; initial_guess_method = OGA1dNormalEquations()).\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA1dNormalEquations"},{"category":"type","location":"#NonlinearIntegrators.OGADictionary","page":"Home","text":"OGADictionary\n\nThe candidate neuron set the greedy step selects from. One of BiasGrid1d, WeightBiasGrid2d, AngularGrid, or any of those wrapped in Refined.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGADictionary"},{"category":"type","location":"#NonlinearIntegrators.OGAFit","page":"Home","text":"OGAFit\n\nHow the OGA refits the output weights of the selected atoms. One of WeightedQR, IncrementalQR, PivotedQR, TruncatedSVD or NormalEquationsFit; see each for the trade-off.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGAFit"},{"category":"type","location":"#NonlinearIntegrators.OGAResult","page":"Home","text":"OGAResult{T}\n\nWhat oga_fit returns.\n\nW, b, c — hidden weights, hidden biases and output weights, one entry per neuron.\natoms — the dictionary indices selected, in order.\nneurons — how many neurons the greedy loop actually placed; a smaller number than requested means it ran out of atoms that add a new direction, and the remainder were filled with zero-weight placeholders (see fill_unused in OGA).\nresidual — the weighted L² norm of the final fit residual.\ngains — the rank gain ‖g⊥‖ of each accepted atom: how much genuinely new direction it contributed. A gain collapsing towards zero across the sequence is the fingerprint of the reduced-precision failure this subsystem exists to remove.\nrejected — how many candidate atoms were skipped for adding no new direction.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGAResult"},{"category":"type","location":"#NonlinearIntegrators.OGASelection","page":"Home","text":"OGASelection\n\nHow the greedy step scores candidate atoms against the current residual. One of RawProjection, NormalizedProjection or OrthogonalProjection.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGASelection"},{"category":"type","location":"#NonlinearIntegrators.OGASymmetry","page":"Home","text":"OGASymmetry\n\nHow one selected dictionary atom turns into network neurons. NoSymmetry is the plain case; the two mirror variants exist for the time-reversible integrators, whose ansatz requires neurons to come in pairs related by t ↦ 1 - t.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGASymmetry"},{"category":"type","location":"#NonlinearIntegrators.OrthogonalProjection","page":"Home","text":"OrthogonalProjection(; min_gain = nothing)\n\nScore by the projection onto the part of the atom orthogonal to the already selected ones, |⟨r, g⟩_w| / ‖g⊥‖_w, and refuse any atom whose orthogonal part has collapsed (‖g⊥‖ < min_gain · ‖g‖); min_gain = nothing uses sqrt(eps(T)).\n\nThis is what makes the algorithm orthogonal greedy rather than matching pursuit, and it is the direct fix for the observed reduced-precision failure. The residual is already orthogonal to the selected span, so the numerator is unchanged from NormalizedProjection — but the denominator penalises an atom that mostly duplicates what is already there, and the min_gain floor rules it out entirely. An atom that adds no new direction is therefore never selected, which is the condition that otherwise surfaces downstream as SingularException: zero pivot found at index 3 out of four neurons.\n\nCosts one dictionary-sized matrix product against the maintained Q per step, the same order as the score itself.\n\n\n\n\n\n","title":"NonlinearIntegrators.OrthogonalProjection"},{"category":"type","location":"#NonlinearIntegrators.PivotedQR","page":"Home","text":"PivotedQR(; rtol = nothing)\n\nRank-revealing Householder QR with column pivoting, truncated below rtol · (largest pivot); rtol = nothing uses eps(T) · max(4, k) with k = min(nnodes, ncols) — see the note above _rtol for why not sqrt(eps(T)).\n\nUnlike WeightedQR, a numerically dependent selected atom is detected and given a zero coefficient rather than solved through. Hand-rolled because qr(Â, ColumnNorm()) is LAPACK-only and so does not exist at Float16 — the precision that needs it.\n\n\n\n\n\n","title":"NonlinearIntegrators.PivotedQR"},{"category":"type","location":"#NonlinearIntegrators.RawProjection","page":"Home","text":"RawProjection()\n\nScore by the bare weighted inner product |⟨r, g⟩_w|.\n\nThe default, and what the Float64/Float32 regression tests pin. Note it is not scale-invariant — an atom with a large norm outranks a better-aligned small one — which is harmless for the {±1} × (bias grid) dictionary, whose atoms all have comparable norms, and wrong for a 2-D (w, b) dictionary, whose atoms do not.\n\n\n\n\n\n","title":"NonlinearIntegrators.RawProjection"},{"category":"type","location":"#NonlinearIntegrators.Refined","page":"Home","text":"Refined(inner; iterations = 3, shrink = 0.5)\n\nWrap any dictionary so that, after the greedy argmax picks a grid atom, the atom's (w, b) are polished off the grid by locally maximising the same selection score.\n\nThe grid then only has to get the neighbourhood right, which decouples accuracy from dictionary size: a few dozen atoms plus refinement can match a dictionary of hundreds of thousands, and the greedy step is linear in the dictionary size. This is the standard \"OGA with inner optimisation\".\n\nThe local search is a derivative-free compass search — evaluate the score at (w ± h, b) and (w, b ± h), step to the best improvement, shrink h by shrink when none improves — repeated iterations times. Derivative-free on purpose: the score is only piecewise smooth for ReLUᵏ (the kink crosses a quadrature node), and it keeps the activation off the ForwardDiff path entirely, so no Dual tag can leak into the working precision.\n\nThe polished objective is always the normalised score, even when the selection rule is RawProjection — see the note in _candidate_score. Maximising the raw inner product continuously over (w, b) would reward growing the atom rather than fitting the residual.\n\n\n\n\n\n","title":"NonlinearIntegrators.Refined"},{"category":"type","location":"#NonlinearIntegrators.SharedMirrorPairs","page":"Home","text":"SharedMirrorPairs()\n\nOne atom → two mirrored neurons → one design column, their sum, so the pair shares a single output weight.\n\nSharing the weight is what actually enforces time-reversal symmetry of the ansatz (with independent weights the pair can drift apart). Used by Time_Reversible_Hardcode.\n\nAs for MirrorPairs, the requested neuron count must be even.\n\n\n\n\n\n","title":"NonlinearIntegrators.SharedMirrorPairs"},{"category":"type","location":"#NonlinearIntegrators.TruncatedSVD","page":"Home","text":"TruncatedSVD(; rtol = nothing)\n\nMinimum-norm solve through a truncated pseudo-inverse, dropping singular directions with σ < rtol · σ_max; rtol = nothing uses eps(T) · max(4, k) with k = min(nnodes, ncols) — see the note above _rtol for why not sqrt(eps(T)).\n\nThe most robust of the fits — a rank-deficient selected set gives a bounded solution instead of amplified rounding noise — and the one to reach for if a single variant must work unchanged across every precision. Uses the generic one-sided Jacobi jacobi_svd, since svd is likewise LAPACK-only.\n\n\n\n\n\n","title":"NonlinearIntegrators.TruncatedSVD"},{"category":"type","location":"#NonlinearIntegrators.WeightBiasGrid2d","page":"Home","text":"WeightBiasGrid2d(; octaves = (-3.0, 3.0), weight_amount = 6, signed = true,\n                   bias_amount = nothing)\n\nA genuine 2-D grid over (w, b): weight_amount + 1 weight magnitudes spaced logarithmically over 2^octaves[1] … 2^octaves[2], optionally sign-symmetric, crossed with the bias grid.\n\nThe weight axis spans length scales by ratio, so the default covers a factor of 64 between the sharpest and gentlest transition. bias_amount overrides the method's dict_amount on the bias axis (nothing keeps it), which is how the total dictionary size is held roughly constant while the weight axis is added — the greedy step is linear in the dictionary size.\n\nSet octaves = (0.0, 0.0), weight_amount = 0 to recover BiasGrid1d exactly: the extra weight degrees of freedom are redundant for ReLUᵏ, so this is a strict generalisation — neutral for the homogeneous activations and enabling for the smooth ones. Pair it with NormalizedProjection: atoms of very different |w| have very different raw norms, so RawProjection would rank them by amplitude rather than by fit.\n\n\n\n\n\n","title":"NonlinearIntegrators.WeightBiasGrid2d"},{"category":"type","location":"#NonlinearIntegrators.WeightedQR","page":"Home","text":"WeightedQR()\n\nQR least squares on the √w-scaled design matrix (see weighted_lstsq): conditioned on κ(Φ) rather than κ(Φ)², with no Gram matrix and no ridge unless the plain solve comes back non-finite.\n\nThe default, and the fit whose behaviour the Float64/Float32 regression tests pin. It re-solves from scratch at each greedy step.\n\n\n\n\n\n","title":"NonlinearIntegrators.WeightedQR"},{"category":"method","location":"#NonlinearIntegrators.OGA1d-Tuple{}","page":"Home","text":"OGA1d(; kwargs...)\n\nThe default seed: the original {±1} × (bias grid) dictionary, raw-projection selection, and a QR fit of the √w-scaled design matrix.\n\nIts atom choices are pinned by the regression tests: normalising before selection steers the Newton solve into a different and empirically worse basin.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA1d"},{"category":"method","location":"#NonlinearIntegrators.OGA1dNormalized-Tuple{}","page":"Home","text":"OGA1dNormalized(; kwargs...)\n\nOGA1d, but selecting on the normalized inner product.\n\nHardcode_int's constructor default: alone among the four integrators it ranks candidates by |⟨r, g⟩_w| / ‖g‖_w rather than by the raw projection. Which of the two an integrator uses changes which neurons get picked and therefore which basin the Newton solve lands in, so each keeps the rule it was tuned with rather than inheriting a single default.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA1dNormalized"},{"category":"method","location":"#NonlinearIntegrators.OGA1dStable-Tuple{}","page":"Home","text":"OGA1dStable(; kwargs...)\n\nThe same 1-D dictionary made robust at reduced precision: orthogonal-greedy selection with a rank-gain floor, on top of the incrementally maintained QR.\n\nThe combination aimed squarely at the 16-bit failure mode: an atom that adds no new direction is never selected, so the selected design matrix cannot go rank-deficient regardless of precision.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA1dStable"},{"category":"method","location":"#NonlinearIntegrators.OGA2d-Tuple{}","page":"Home","text":"OGA2d(; dictionary = WeightBiasGrid2d(), kwargs...)\n\nA 2-D (w, b) dictionary with normalised selection and the incremental QR fit: the variant for activations that are not positively homogeneous (ELU, GELU, tanh), where |w| is a genuine length-scale degree of freedom rather than redundant with b and c.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGA2d"},{"category":"method","location":"#NonlinearIntegrators.OGASphere-Tuple{}","page":"Home","text":"OGASphere(; dictionary = AngularGrid(), kwargs...)\n\nAtoms sampled uniformly on the sphere of (w, b) space rather than uniformly in bias — the dictionary the underlying approximation theory is stated for. See AngularGrid.\n\n\n\n\n\n","title":"NonlinearIntegrators.OGASphere"},{"category":"method","location":"#NonlinearIntegrators.bias_grid-Union{Tuple{T}, Tuple{Any, Any, Integer, Type{T}}} where T","page":"Home","text":"bias_grid(lo, hi, n, ::Type{T}) -> Vector{T}\n\nUniform grid of n + 1 bias values from lo to hi in precision T.\n\nThe coordinates are generated from an integer-indexed range in Float64 and then cast to T, so a large n cannot overflow the step to zero in reduced precision: at Float16, T(n) overflows to Inf for n > 65504 and (hi - lo)/n evaluates to zero, which a range built in T rejects with ArgumentError: range step cannot be zero. Only the grid coordinates touch Float64; the seed's dictionary and solve run entirely in T.\n\n\n\n\n\n","title":"NonlinearIntegrators.bias_grid"},{"category":"method","location":"#NonlinearIntegrators.initial_params!-Tuple{GeometricIntegratorsBase.GeometricIntegrator{var\"#s101\", PT, CT} where {var\"#s101\"<:NonLinear_OneLayer_GML, PT<:GeometricBase.AbstractProblem, CT<:GeometricIntegratorsBase.CacheDict{PT, var\"#s101\"}}, OGA1dNormalEquations, Any}","page":"Home","text":"initial_params!(int, ::OGA1dNormalEquations, sol)\n\nReference OGA initial guess for NonLinear_OneLayer_GML: the implementation from the original paper, kept as a selectable baseline.\n\nThe dictionary and the greedy least-squares fit are assembled in Float64 (a \"double-precision island\"), the output weights come from the normal equations Gk \\ rhs, and the result is rounded into the working-precision cache. There is no norm floor, no coherence guard, no ridge and no rank detection. See the \"Orthogonal Greedy Algorithm\" section of the documentation for why the working-precision QR fit of OGA1d replaced it as the default, and OGA1dNormalEquations for the failure mode this variant exhibits at 16 bits.\n\nDefined only for NonLinear_OneLayer_GML — it is a comparison baseline, not a production seed.\n\n\n\n\n\n","title":"NonlinearIntegrators.initial_params!"},{"category":"method","location":"#NonlinearIntegrators.initial_trajectory!-Tuple{Any, Any, Any, GeometricIntegratorsBase.GeometricIntegrator, GeometricIntegratorsBase.Extrapolation}","page":"Home","text":"initial_trajectory!(sol, history, params, int, initial_trajectory)\n\nInitial trajectory for the NonLinear_OneLayer_Lux integrator.\n\n\n\n\n\n","title":"NonlinearIntegrators.initial_trajectory!"},{"category":"method","location":"#NonlinearIntegrators.jacobi_svd-Union{Tuple{AbstractMatrix{T}}, Tuple{T}} where T","page":"Home","text":"jacobi_svd(Â; sweeps = 30) -> (σ, U, V)\n\nSingular values and factors of Â (nodes × atoms, tall or square) by the one-sided Jacobi method: orthogonalise the columns pairwise by plane rotations until they are mutually orthogonal, at which point the column norms are the singular values, the normalised columns are U, and the accumulated rotations are V.\n\nOne-sided Jacobi is chosen over bidiagonalisation because it is short, generic in eltype(Â) (LinearAlgebra's svd is LAPACK-only), and has high relative accuracy on the small singular values — exactly the ones that decide whether the greedily selected atoms are still independent at reduced precision. At k ≤ 8 columns a handful of sweeps converges and the cost is irrelevant.\n\nσ is returned in the column order of V, not sorted.\n\n\n\n\n\n","title":"NonlinearIntegrators.jacobi_svd"},{"category":"method","location":"#NonlinearIntegrators.oga_atoms-Union{Tuple{T}, Tuple{BiasGrid1d, Any, Integer, Type{T}}} where T","page":"Home","text":"oga_atoms(dict, bias_interval, dict_amount, ::Type{T}) -> Matrix{T}\n\nBuild the candidate atom matrix: one row per atom, column 1 the hidden weight w and column 2 the bias b, in precision T.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_atoms"},{"category":"method","location":"#NonlinearIntegrators.oga_check_neuron_count-Tuple{Int64, OGASymmetry}","page":"Home","text":"oga_check_neuron_count(nneurons, symmetry)\n\nAssert that nneurons is a multiple of neurons_per_atom(symmetry).\n\nThe loop places whole atoms, so under a mirrored symmetry an odd nneurons runs nneurons ÷ 2 steps and leaves the last neuron at (0, 0); _fill_unused! fills pairs too, so it cannot repair it. That is the duplicated-neuron state the fill exists to avoid, where a rank-deficient seed becomes a rank-deficient Newton Jacobian.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_check_neuron_count"},{"category":"method","location":"#NonlinearIntegrators.oga_check_precision-Union{Tuple{T}, Tuple{Any, Type{T}}} where T","page":"Home","text":"oga_check_precision(σ, ::Type{T})\n\nAssert that the activation evaluates at the working precision, i.e. that σ(::T) is a T.\n\nThis is the one trap that silently invalidates a reduced-precision run: an activation written max(0.0, x)^k instead of max(zero(x), x)^k promotes every evaluation to Float64, so the seed is computed in double precision and the measurement says nothing about T. It costs one scalar call per fit to rule out, and the failure it catches is otherwise visible only as suspiciously good half-precision accuracy.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_check_precision"},{"category":"method","location":"#NonlinearIntegrators.oga_fit-Union{Tuple{T}, Tuple{OGA, Any, AbstractVector{T}, AbstractVector{T}, AbstractVector{T}, Int64}} where T","page":"Home","text":"oga_fit(oga, σ, nodes, w, y, nneurons; bias_interval, dict_amount,\n        modulation = nothing, symmetry = NoSymmetry()) -> OGAResult\n\nGreedily fit nneurons neurons of a one-layer network to the target y sampled at nodes, under the quadrature weights w.\n\noga::OGA — the dictionary, selection rule, fit and guard rails.\nσ — the activation; must evaluate at eltype(nodes) (checked, see oga_check_precision).\nmodulation — optional per-node factor multiplying every atom, for the boundary ansatz q(t) = (1-t) q̄ + t q + t(1-t) u(t), where the dictionary is t(1-t) σ(w t + b). Pass the t(1-t) vector; nothing means no modulation.\nsymmetry::OGASymmetry — how atoms map to neurons.\n\nnneurons must be a multiple of neurons_per_atom(symmetry) — even, for the two mirrored symmetries. An odd count is an ArgumentError rather than a silently short fit; see oga_check_neuron_count.\n\nRuns entirely at T = eltype(nodes).\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_fit"},{"category":"method","location":"#NonlinearIntegrators.oga_norm_floor-Union{Tuple{T}, Tuple{Type{T}, Any}} where T","page":"Home","text":"oga_norm_floor(::Type{T}, ref) -> T\n\nAmplitude-scale floor below which a dictionary-atom norm is treated as numerically zero (so normalization is skipped rather than dividing by noise). Scales with sqrt(eps(T)) * ref, i.e. relative to the largest atom ref and to the working precision: a norm smaller than this cannot be inverted reliably in T.\n\nReplaces the hard-coded absolute 1e-12 guard, which sat below eps(Float32) and so never fired in reduced precision.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_norm_floor"},{"category":"method","location":"#NonlinearIntegrators.oga_qr_append!-Union{Tuple{T}, Tuple{NonlinearIntegrators.IncrementalQRState{T}, AbstractVector{T}}} where T","page":"Home","text":"oga_qr_append!(qr, a; min_gain) -> ρ\n\nAppend column a to the factorisation and return the deflated norm ρ = ‖a⊥‖, the part of a orthogonal to the columns already present. The column is rejected (nothing is appended, ρ still returned) when ρ ≤ min_gain · ‖a‖, i.e. when a lies in the existing span to within the requested tolerance — the rank drop that otherwise surfaces downstream as SingularException: zero pivot found at index 3.\n\nOrthogonalisation is modified Gram–Schmidt with one reorthogonalisation pass. That second pass is not optional bookkeeping: plain MGS loses orthogonality in proportion to the condition number, which at Float16 is the whole problem, whereas reorthogonalising once restores it to O(eps(T)) for any conditioning that has not already collapsed.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_qr_append!"},{"category":"method","location":"#NonlinearIntegrators.oga_qr_reset!-Union{Tuple{NonlinearIntegrators.IncrementalQRState{T}}, Tuple{T}} where T","page":"Home","text":"oga_qr_reset!(qr)\n\nDrop all columns, reusing the storage. Called once per dimension d of the fit.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_qr_reset!"},{"category":"method","location":"#NonlinearIntegrators.oga_qr_solve-Union{Tuple{T}, Tuple{NonlinearIntegrators.IncrementalQRState{T}, AbstractVector{T}}} where T","page":"Home","text":"oga_qr_solve(qr, ŷ) -> Vector\n\nLeast-squares coefficients from the maintained factorisation: one triangular solve R z = Qᵀ ŷ. Falls back to ridged_lstsq on the reconstructed design matrix if the triangular solve is not finite, so this can never return a non-finite seed.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_qr_solve"},{"category":"method","location":"#NonlinearIntegrators.oga_refine-Union{Tuple{T}, Tuple{OGADictionary, Any, T, T}} where T","page":"Home","text":"oga_refine(dict, score, w, b) -> (w, b)\n\nPolish a selected atom off the grid. score(w, b) returns the selection score of the candidate atom (larger is better; -Inf/NaN for an invalid one). The default is a no-op, so only Refined does any work.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_refine"},{"category":"method","location":"#NonlinearIntegrators.oga_scores!-Union{Tuple{T}, Tuple{AbstractVector{T}, RawProjection, AbstractMatrix{T}, AbstractVector{T}, T, AbstractVector{T}, NonlinearIntegrators.IncrementalQRState{T}, AbstractMatrix{T}}} where T","page":"Home","text":"oga_scores!(score, rule, Ψ, rownorms, nfloor, r̂, qr, proj)\n\nFill score with the selection score of every dictionary atom.\n\nΨ — (natoms × nnodes) √w-scaled dictionary, one atom per row.\nrownorms — the √w-weighted L² norm of each atom.\nnfloor — norm floor below which an atom counts as numerically zero.\nr̂ — the √w-scaled current residual.\nqr — the factorisation of the selected columns (used by OrthogonalProjection).\nproj — (natoms × maxcols) scratch for Ψ Q.\n\nReturns score. Unusable atoms are set to -one(T).\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_scores!"},{"category":"method","location":"#NonlinearIntegrators.oga_seed-Tuple{GeometricIntegratorsBase.GeometricIntegrator, OGA, OGASymmetry, AbstractVector, Any}","page":"Home","text":"oga_seed(int, oga, symmetry, targets, modulation) -> Vector{OGAResult}\n\nRun the greedy fit once per solution component, sharing the dictionary configuration and quadrature taken from the integrator's method.\n\ntargets[d] is the fit target for component d at the network's input nodes, and modulation is the optional per-node ansatz factor (see oga_fit).\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_seed"},{"category":"method","location":"#NonlinearIntegrators.oga_solve-Union{Tuple{T}, Tuple{OGAFit, AbstractMatrix{T}, AbstractVector{T}, NonlinearIntegrators.IncrementalQRState{T}}} where T","page":"Home","text":"oga_solve(fit, Â, ŷ, qr) -> Vector\n\nRefit the output weights of the currently selected atoms. Â (nnodes × k) is the √w-scaled design matrix, ŷ the √w-scaled target, and qr the incrementally maintained factorisation of Â (used only by IncrementalQR, but kept in the signature so every fit is interchangeable).\n\nGuarantees, for every fit, at every precision: the result has one entry per column of Â and every entry is finite. That is enforced here rather than in each fit, so it holds for a new fit by construction. Where a factorisation throws on a rank-deficient design, or returns Inf/NaN from a division by a pivot that survived truncation, the fit falls back to the ridged solve of ridged_lstsq. A seed the Newton solve can start from is worth more than a faithful report that the fit was impossible — and the rank-deficiency itself is already reported, through OGAResult's gains and rejected.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_solve"},{"category":"method","location":"#NonlinearIntegrators.oga_tikhonov-Union{Tuple{AbstractMatrix{T}}, Tuple{T}} where T","page":"Home","text":"oga_tikhonov(G; C = 100) -> eltype(G)\n\nScale- and precision-relative Tikhonov floor C * eps(T) * tr(G)/n for stabilizing a Gram / normal-equations solve G \\ rhs at working precision T = eltype(G). Relative to the mean diagonal tr(G)/n, so the effective condition-number cap tracks the precision; C is a modest safety factor.\n\nReplaces the hard-coded absolute 1e-12 / 1e-14 ridges, which round away entirely below eps(Float32). Used by NormalEquationsFit as its ridge; the default OGA fit is weighted_lstsq, which avoids forming G at all.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_tikhonov"},{"category":"method","location":"#NonlinearIntegrators.pivoted_qr_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}, T}} where T","page":"Home","text":"pivoted_qr_lstsq(Â, ŷ, rtol) -> Vector\n\nRank-revealing least squares by Householder QR with column pivoting, truncated where the pivot norm drops below rtol times the first (largest) pivot.\n\nThe pivoting is what makes it rank-revealing: at each step the column with the largest remaining norm is brought forward, so a numerically dependent column is pushed to the end and detected by its collapsed pivot rather than being solved through. Columns past the detected rank get a zero coefficient — the returned vector always has one entry per input column, so callers need not know the rank.\n\nGeneric in eltype(Â); see the note above on why qr(Â, ColumnNorm()) cannot be used.\n\n\n\n\n\n","title":"NonlinearIntegrators.pivoted_qr_lstsq"},{"category":"method","location":"#NonlinearIntegrators.ridged_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}}} where T","page":"Home","text":"ridged_lstsq(Â, ŷ; C = 100) -> Vector\n\nTikhonov-ridged least squares for the √w-scaled design matrix Â, solved as the augmented QR problem [Â; √λ I] x ≈ [ŷ; 0] rather than on the normal equations, so the ridge stabilises the solve without squaring the condition number. The ridge λ = C · eps(T) · tr(ÂᵀÂ)/natoms is the precision-scaled floor of oga_tikhonov.\n\n\n\n\n\n","title":"NonlinearIntegrators.ridged_lstsq"},{"category":"method","location":"#NonlinearIntegrators.scaled_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}}} where T","page":"Home","text":"scaled_lstsq(Â, ŷ; C = 100) -> Vector\n\nPlain QR least squares Â \\ ŷ with the precision-scaled ridged retry, for an already √w-scaled design matrix Â (nodes × atoms) and target ŷ.\n\nThis is the body of weighted_lstsq split out so that the greedy loop, which works entirely in the √w-scaled space, does not re-scale on every iteration. The ridged retry is shared by every OGAFit as a last resort, so no variant can return a non-finite seed.\n\n\n\n\n\n","title":"NonlinearIntegrators.scaled_lstsq"},{"category":"method","location":"#NonlinearIntegrators.truncated_svd_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}, T}} where T","page":"Home","text":"truncated_svd_lstsq(Â, ŷ, rtol) -> Vector\n\nMinimum-norm least squares via the truncated pseudo-inverse: singular directions with σ < rtol · σ_max are dropped instead of divided by, so a rank-deficient selected set yields a bounded solution rather than amplified rounding noise. The most robust of the fits, and the one to reach for when a variant must never fail at any precision.\n\nUses jacobi_svd, so it is generic in eltype(Â).\n\n\n\n\n\n","title":"NonlinearIntegrators.truncated_svd_lstsq"},{"category":"method","location":"#NonlinearIntegrators.weight_grid-Union{Tuple{T}, Tuple{Any, Any, Integer, Type{T}}} where T","page":"Home","text":"weight_grid(lo_octave, hi_octave, n, ::Type{T}) -> Vector{T}\n\nLogarithmically spaced grid of n + 1 positive weight magnitudes 2^lo_octave … 2^hi_octave in precision T, for the weight axis of a 2-D (w, b) dictionary.\n\nSpacing is logarithmic because w sets the length scale of a non-homogeneous activation (how sharply the unit transitions), and length scales are naturally compared by ratio rather than by difference. The exponents are generated by the same integer-indexed Float64 range as bias_grid, so a large n cannot collapse the step in reduced precision, and the exponentiation is done in Float64 before the single cast to T so that an octave outside the range of T saturates predictably instead of overflowing mid-computation.\n\n\n\n\n\n","title":"NonlinearIntegrators.weight_grid"},{"category":"method","location":"#NonlinearIntegrators.weighted_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}, AbstractVector{T}}} where T","page":"Home","text":"weighted_lstsq(Φ, w, y; C = 100) -> Vector\n\nSolve the (ridged) quadrature-weighted least-squares fit for the output weights x,\n\nminₓ  Σⱼ wⱼ (Σᵢ xᵢ Φ[i,j] − yⱼ)²  +  λ‖x‖² ,\n\nwhere each row of Φ is a dictionary atom sampled at the quadrature nodes, w are the (positive) quadrature weights, and y is the fit target at those nodes.\n\nSolved by QR on the √w-scaled design matrix rather than the normal equations Φ diag(w) Φᵀ, so accuracy is governed by κ(Φ) instead of κ(Φ)² — the difference that lets the fit run in reduced precision without a rank-deficient Gram matrix.\n\nThe plain QR solve is used whenever it is finite (so the Float64/Float32 atom choice matches the Gram solution bit for bit and the greedy residual is unperturbed). Only if it returns a non-finite result — a genuinely rank-deficient design matrix, i.e. the Float16 case — is the fit retried with a √λ·I augmentation, where the ridge λ = C · eps(T) · tr(ÂᵀÂ)/natoms is the precision-scaled Tikhonov floor (see oga_tikhonov) that keeps the solution bounded and the seed finite.\n\n\n\n\n\n","title":"NonlinearIntegrators.weighted_lstsq"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Studies","page":"Studies","text":"Three studies measure the variants, in scripts/ (see scripts/README.md for how to run them). They are studies of one component rather than benchmarks of the integrator suite, which is what benchmark/ holds, and they are not part of the documentation build.","title":"Studies"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Why-two-tiers","page":"Studies","text":"End-to-end convergence conflates the quality of the seed with the behaviour of the solve, and that confound is what made these failures hard to attribute in the first place: a run that fails looks identical whether the greedy fit went rank-deficient or the Newton Jacobian did. So the measurement is split.\n\nTier A — seed quality (oga_fit_study.jl) calls oga_fit directly: no integrator, no Newton solve, no time stepping. It sweeps dictionary × selection × fit × activation × precision × target and reports\n\nfit_err — the quadrature-weighted L^2 error of the seed, recomputed in Float64 after the fit from the returned parameters, so precisions share one scale;\ncond / sigma_min — of the seed's design matrix (all S neurons, including any zero-weight placeholders, since those are part of what the Newton solve sees). This is the proxy for whether the system the seed feeds is solvable;\nneurons / rejected — how many of the requested neurons the greedy loop could place, and how many candidates it refused for adding no new direction.\n\nEvery case is an S le 8, 11-node problem, so ~6000 cases run in seconds. This is the tier that actually separates the variants.\n\nTier B — end-to-end (oga_sweep.jl) integrates the harmonic oscillator for ten steps at S = 4, R = 8, dt = 0.1 with DogLeg, over seed variant × precision × regularization factor × activation, in two stages matching the two questions:\n\nrelu — ReLUᵏ for k = 1…4, where the {±1} dictionary is theoretically complete, so anything that goes wrong is numerical (the reduced-precision question);\nsmooth — ELU, GELU and tanh against the 2-D and angular dictionaries built for them (the activation question).\n\nTier B′ — the hardest problem (oga_double_pendulum.jl) repeats a reduced grid on the double pendulum at a single λ, read from the harmonic-oscillator sweep's CSV — the factor that converged most often there — so the choice is measured rather than asserted.","title":"Why two tiers"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Methodology","page":"Studies","text":"Three points that decide whether the numbers mean anything.","title":"Methodology"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Everything-absolute-must-be-scaled-to-the-precision","page":"Studies","text":"regularization_factor is swept as multiples of sqrtvarepsilon(T), not as absolute values, so the Jacobian-diagonal shift is scaled to the precision it protects. Each value is identified by that multiple, which is also how the reports label it — lambda = 16sqrtvarepsilon(T) says what the shift is:\n\nprecision multiples swept lambda range\nFloat16 2, 4, 8, 16, 32, 64 6.3e-2 … 2.0\nFloat32 2, 4, 8, 16, 32, 64 6.9e-4 … 2.2e-2\nFloat64 4, 16, 64, 256, 1024, 4096 6.0e-8 … 6.1e-5\n\nplus a lambda = 0 control (multiple 0). The Float64 set is stretched so that all three ladders span a comparable dynamic range; 16sqrtvarepsilon(T) appears in each and is the value the package documents as its default.\n\nAn absolute 1e-5 sits far below sqrtvarepsilon at anything but Float64 and so cannot lift a near-singular Jacobian in reduced precision at all — which is why the sweep is over multiples rather than over fixed numbers.\n\nThe same applies to f_abstol, and there getting it wrong invalidates a whole column rather than degrading it — see Precision. The studies pass f_textabstol = 256varepsilon(T).\n\nwarning: How to read the precision columns\nBecause the tolerance is scaled per precision, each column answers \"did it reach its own precision's floor?\" — not \"did it reach the same absolute accuracy?\". The three targets areprecision 256varepsilon(T)\nFloat16 0.25\nFloat32 3.05e-5\nFloat64 5.68e-14which differ by twelve orders of magnitude. Two consequences, and both matter for reading the tables below.A Float32 or Float16 convergence count can legitimately exceed the Float64 one, and does; that is the looser target, not greater robustness. And at Float16 the target is so loose — a residual of 0.25 — that \"converged\" carries little information on its own: the column separates outright failures (a thrown SingularException, a divergence) from everything else, and not much more. Compare variants within a column, weight the Float64 column most heavily, and read the accuracy figures alongside the counts.This is the unavoidable tension in a precision sweep: a tolerance tight enough to be meaningful at Float64 measures only the tolerance at Float16, and one loose enough to be reachable at Float16 measures almost nothing there. Scaling it at least makes the failure mode explicit rather than silent.","title":"Everything absolute must be scaled to the precision"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#A-finite-result-is-not-a-converged-one","page":"Studies","text":"integrate returns a finite state after exhausting max_iterations, so classifying on isfinite alone records stalls as successes — the \"finite-but-poor result slips under a relaxed tolerance\" hazard. Runs that exhaust the budget are recorded as maxiter, not ok, with their accuracy still reported so a stall can be told apart from a divergence. A run whose final state has left the working precision is recorded as upcast.","title":"A finite result is not a converged one"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Diagnostics-are-quarantined","page":"Studies","text":"cond, sigma_min and the comparable fit_err are computed in Float64 after oga_fit returns, purely for reporting. Nothing derived from them re-enters the fit.","title":"Diagnostics are quarantined"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Figures","page":"Studies","text":"scripts/oga_report.jl writes the CSVs, markdown reports and PNGs, and the reports regenerate from the CSVs alone — so a table or figure can be reworked without re-running a sweep.\n\nThree conventions in the figures, and they are not stylistic:\n\nmagnitude on a single-hue sequential ramp, light to dark. Specifically not the red→green ramp that success-rate grids usually reach for: red↔green is the one pair red–green colourblind readers cannot separate, which is about 8% of men;\nevery heatmap cell also carries its numeric value, so nothing is encoded by colour alone and the figure doubles as a table;\na fixed categorical order for precision, so a given precision keeps its colour across every figure regardless of which ones a particular sweep produced, with lines direct-labelled as well as legended.\n\nOne trap worth recording: the colour range must come from the plotted medians, not from the raw rows. Ranging over the raw data stretches the scale to cover per-activation spread that no cell displays, flattening every median into one indistinguishable shade.","title":"Figures"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#What-the-studies-found","page":"Studies","text":"","title":"What the studies found"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Tier-A-—-seed-quality","page":"Studies","text":"Across 6048 cases at Float16/Float32/Float64 there were no failures: every (dictionary, selection, fit) combination returned a finite seed at every precision. That is the guarantee oga_solve enforces, and it holds.\n\nThe kappa^2 penalty is directly visible. At Float16, the working-precision Gram solve (normaleq) is consistently three to four times worse than every QR-family fit — e.g. median fit error 2.10e-01 against 5.67e-02 on the angular dictionary with normalised selection — while the same Gram solve with the Float64 island restored (normaleq+f64) recovers to 5.67e-02, matching QR. That is the island's contribution isolated in one row pair: it was buying robustness of the squared-condition-number solve, exactly as the analysis predicts, and nothing else.\n\nTwo patterns in the best-variant-per-cell table:\n\northogonal selection wins the large majority of cells, at every precision;\nfor the smooth activations (elu, gelu, tanh) the winner is a 2-D dictionary (grid2d or angular) in every case — never the 1-D bias grid. The angular grid also wins several ReLUᵏ cells, which the non-uniform-coverage argument in Theory predicts.\n\nThe rank statistics confirm both guard-rail claims quantitatively. Out of 672 cases per (precision, selection) group:\n\nprecision selection short of full width cases with rejections most rejected non-finite\nFloat16 raw 49 22 41 0\nFloat16 normalized 49 31 40 0\nFloat16 orthogonal 210 199 41 0\nFloat32 any 0 0–3 ≤4 0\nFloat64 any 0 0–2 ≤20 0\n\nAt Float16, OrthogonalProjection refuses a rank-deficient atom in 199 of 672 cases and consequently places fewer than four neurons in 210 — it is doing exactly the job it exists for, and the cost is visible as reduced width rather than hidden as a bad fit. At Float32 and Float64 the guards are essentially inert, which is the claim made for them: distinct atoms are well separated there, so nothing gets blocked. And non-finite is 0 in all nine groups, across all 6048 cases — the oga_solve finiteness guarantee holding under measurement rather than assertion.","title":"Tier A — seed quality"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Tier-B-—-ReLUᵏ-end-to-end","page":"Studies","text":"Converged runs out of 28 per precision (four ReLU powers × seven regularization factors). Because the sweep pins its own residual tolerance, these counts are independent of the integrator's default and reproduce exactly across solver versions:\n\nseed Float16 Float32 Float64 median err (converged)\nreference (OGA1dNormalEquations) 0/28 18/28 15/28 4.47e-07\noga1d (OGA1d) 19/28 25/28 17/28 4.78e-05\noga1d-stable (OGA1dStable) 18/28 26/28 21/28 4.78e-05\noga1d-pivqr (orthogonal + PivotedQR) 18/28 26/28 20/28 6.96e-05\noga1d-tsvd (orthogonal + TruncatedSVD) 18/28 26/28 23/28 4.78e-05\noga1d-refined (Refined + normalised + incremental QR) 22/28 26/28 20/28 6.95e-05\n\nThree things to read out of it.\n\nThe reference implementation converges nowhere at Float16 — 0 out of 28, at every ReLU power. It fails by throwing (SingularException from the Gram solve), so unlike the counts around it this zero is not an artefact of the residual tolerance: no choice of target rescues it. It is also the worst variant at Float32 and Float64 (18 and 15, against 25–26 and 17–23). This answers the reduced-precision question.\n\nThe rank-revealing fits are the most robust, and by a clear margin at Float64. oga1d-tsvd reaches 23/28 there against the default's 17 and the reference's 15; oga1d-stable and oga1d-pivqr sit just behind at 21 and 20. Off-grid refinement is the strongest at Float16 (22/28). The ordering is consistent enough across the three columns to be a real effect rather than noise — every orthogonal-selection variant beats the default at Float64, and every working-precision variant beats the reference everywhere.\n\nThe reference nevertheless produces the most accurate seed where it works at all — median 4.47e-07, two orders of magnitude better than the 4.8e-05 to 7.0e-05 of the rest. Its Float64 seed genuinely is a better warm start. That is the one column where the island pays for itself, and it is worth stating plainly: the working-precision variants buy robustness, not accuracy.\n\nPer activation, ReLU⁴ is the hard case — 4/42 at Float16 and 13/42 at Float64, against 28–39/42 for the lower powers — which is unsurprising, since σ(b)⁴ over [-π, π] spans eight orders of magnitude and overflows Float16 outright.","title":"Tier B — ReLUᵏ end-to-end"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#The-regularization-factor","page":"Studies","text":"The sweep reproduces, independently and with the seed variant as an extra axis, what SolverBenchmark found: λ is a threshold, not a tuned value.\n\nprecision lambda = 0 lambda  0 median iterations, lambda = 0 → lambda  0\nFloat16 1/24 15–16/24 4 → 1\nFloat32 11/24 21–23/24 32 → 3–12\nFloat64 4/24 16–22/24 4.5 → 5–9\n\nAt lambda = 0 convergence collapses; every nonzero factor recovers it, and the six factors are barely distinguishable from one another across a range spanning a factor of 32 (Float16/Float32) or 1024 (Float64). The Float32 iteration count is the clearest signal: 32 iterations per step undamped against 3–12 with any damping.\n\nOne caveat on the Float64 row: convergence rises with the factor (17 → 22 from 4√eps to 4096√eps) but median accuracy degrades with it (4.0e-09 → 1.3e-05), because the shift that makes the Jacobian solvable also biases the solution. The largest factor is not the best choice; it is the one that converges most often.","title":"The regularization factor"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Tier-B-—-smooth-activations","page":"Studies","text":"This is the smooth-activation question. Converged runs out of 21 per precision, for ELU, GELU and tanh:\n\nseed dictionary Float16 Float32 Float64\noga1d (OGA1d) 1-D bias grid 18/21 20/21 6/21\noga1d-stable (OGA1dStable) 1-D bias grid 18/21 20/21 7/21\noga-sphere (OGASphere) angular grid 12/21 20/21 7/21\noga2d (OGA2d) 2-D (w, b) grid 18/21 21/21 9/21\noga2d-refined 2-D grid + off-grid polish 18/21 20/21 10/21\n\nRead the Float64 column — it is the only one where the residual target is tight enough to discriminate (see the warning above; at Float16 the target is a residual of 0.25). There the 2-D dictionaries lead: 10/21 and 9/21 against 6/21 and 7/21 for the 1-D grid, with the angular grid at 7/21.\n\nSo the weight axis helps, in the predicted direction and for the predicted reason — but the effect is a factor of about 1.5, not the categorical difference the theory might suggest. Notably, changing the selection rule and fit on the 1-D grid (oga1d → oga1d-stable) moves it 6 → 7, while changing the dictionary moves it 6 → 9–10; so the dictionary is the more effective lever, which is the structural claim. Absolute rates stay low — 6–10 out of 21 — so something beyond the dictionary is also limiting smooth activations at double precision.","title":"Tier B — smooth activations"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Tier-B′-—-double-pendulum","page":"Studies","text":"The grid here is deliberately small: 3 activations × 6 seeds × 1 regularization factor per precision, 54 runs total. That is enough to check whether the reduced-precision failure reproduces on a harder problem, and not enough to rank the variants — the working-precision seeds land on 2/3 at Float16 and Float64 and on 1/3 at Float32 (oga-sphere on 2/3 throughout), so the whole spread between variants is one or two cases.\n\nWhat it does show:\n\nThe reference fails at Float16 here too (0/3, by SingularException), while every working-precision variant manages 2/3. Same failure, different problem. At Float32 and Float64 the reference converges as often as the rest (1/3 and 2/3), so it is half precision specifically that defeats it.\nThe accuracy ordering matches the harmonic oscillator's: the reference has the best median error where it converges (2.57e-05), then oga1d (9.15e-04), then the 2-D dictionaries (9.5e-03–1.0e-02), then oga1d-stable and oga1d-refined (2.1e-02–2.7e-02). The robustness/accuracy trade-off is consistent across both problems.\nThe chosen factors, taken from the harmonic-oscillator sweep, were 2√eps(T) at Float16, 4√eps(T) at Float32 and 4096√eps(T) at Float64.","title":"Tier B′ — double pendulum"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Where-measurement-disagreed-with-the-design","page":"Studies","text":"The remedies implemented here were laid out in advance, in rough order of expected impact: switch the fit to weighted QR and add normalisation plus a coherence guard first; add a truncated SVD \"only if you find a residual case that still degrades\"; and reserve the incremental QR \"for when the per-step re-solve shows up in profiling\".\n\nTwo parts of that guidance did not survive contact with the numbers.\n\nThe rank-revealing fits were not a last resort. They are the most robust variants at Float64 — TruncatedSVD at 23/28 against the QR default's 17 — so the ordering that put them behind the main fix was wrong. The cost reasoning that motivated deferring them was also beside the point: as the note at the top of Algorithms records, the fit is free at this problem size, so there was never a performance reason to prefer a cheaper factorisation.\n\nNormalising the dictionary before selection was expected to be a straightforward improvement. It is not: it changes which atoms are chosen, and at Float64 with ReLUᵏ that steers the Newton solve into a worse basin. That is why RawProjection remains the default and normalised selection is an explicit opt-in, and why OGA1d's selected atoms are pinned by a regression test rather than left free to drift.\n\nThe parts that held: the kappa^2 diagnosis and the QR reformulation, the precision-scaled guard rails replacing the absolute constants, and the incremental QR being both cheaper and more stable.","title":"Where measurement disagreed with the design"},{"category":"section","location":"Orthogonal Greedy Algorithm/studies/#Caveats","page":"Studies","text":"Tier B fixes S = 4, R = 8, dt = 0.1 and DogLeg, so it says nothing about how the ordering varies with network width, quadrature order, step size or solver strategy.\nThe regularization-factor counts are aggregated over seeds and activations, so a cell of 12/24 mixes variants with very different behaviour; the per-seed table is the one to compare within.\nReLU¹ is not C^1, so a Newton stall there is expected rather than informative; it is included as the k = 1 end of the ReLUᵏ axis.\nTier A's targets stand in for the integrator's label sets rather than being sampled from them, so its fit errors are comparable to each other but not to an integrator's accuracy.","title":"Caveats"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Usage","page":"Usage","text":"","title":"Usage"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Presets","page":"Usage","text":"Five named configurations cover the useful corners. All are keyword functions returning an OGA, so they compose with the guard-rail keywords.\n\nPreset Dictionary Selection Fit\nOGA1d BiasGrid1d RawProjection WeightedQR\nOGA1dNormalized BiasGrid1d NormalizedProjection WeightedQR\nOGA1dStable BiasGrid1d OrthogonalProjection IncrementalQR\nOGA2d WeightBiasGrid2d NormalizedProjection IncrementalQR\nOGASphere AngularGrid NormalizedProjection IncrementalQR\n\nOGA1dNormalEquations is the exception: a separate implementation rather than a preset, kept unchanged so that claims about the modern variants are measured against the published algorithm rather than a paraphrase of it. It is available for NonLinear_OneLayer_GML only, and it carries none of the guard rails.\n\nusing NonlinearIntegrators, QuadratureRules\n\nrelu3(x) = max(zero(x), x)^3                       # float-generic — see Precision\nbasis = OneLayerNetwork_GML{Float64}(relu3, 4)     # S = 4 hidden neurons\nquad  = QuadratureRules.GaussLegendreQuadrature(Float64, 8)\n\n# The default: OGA1d()\nmethod = NonLinear_OneLayer_GML(basis, quad; bias_interval = [-π, π], dict_amount = 400)\n\n# A different preset\nmethod = NonLinear_OneLayer_GML(basis, quad; bias_interval = [-π, π], dict_amount = 400,\n                                initial_guess_method = OGA1dStable())\n\n# The original-paper reference, for comparison\nmethod = NonLinear_OneLayer_GML(basis, quad; bias_interval = [-π, π], dict_amount = 400,\n                                initial_guess_method = OGA1dNormalEquations())","title":"Presets"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Composing-your-own","page":"Usage","text":"The presets are corners of one type; any combination of the three axes is equally valid.\n\n# Rank-revealing fit on the classical dictionary\nOGA(BiasGrid1d(), OrthogonalProjection(), TruncatedSVD())\n\n# 2-D dictionary with a wider weight range and a trimmed bias axis, so the total atom\n# count stays comparable to the 1-D grid (the greedy step is linear in it)\nOGA(WeightBiasGrid2d(octaves = (-4, 4), weight_amount = 8, bias_amount = 44),\n    NormalizedProjection(), IncrementalQR())\n\n# Off-grid refinement on a deliberately coarse grid\nOGA(Refined(BiasGrid1d(); iterations = 5), NormalizedProjection(), IncrementalQR())\n\n# Ablations: turn the guard rails off one at a time\nOGA(BiasGrid1d(), RawProjection(), WeightedQR(); coherence = false)\nOGA(BiasGrid1d(), RawProjection(), NormalEquationsFit(ridge = false, island = true))\n\nThe dictionary's bias axis is configured on the method (bias_interval, dict_amount), since those predate this subsystem and every integrator already carries them. Axes that only some dictionaries have — the weight range, the radii, the angular resolution — live on the dictionary object, so no integrator struct had to change.\n\nBecause OGA{D,S,F} holds only singletons and immutables it stays isbits, so the method struct's type parameter remains concrete and there is no dispatch cost.","title":"Composing your own"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Choosing-a-variant","page":"Usage","text":"A short decision guide; the numbers behind it are in Studies.\n\nReLUᵏ at Float64 — OGA1d. The classical dictionary is complete, and the pinned raw-projection selection is what the accuracy guards were tuned against.\nReLUᵏ at 16 bits — OGA1dStable, or Refined(BiasGrid1d()). The rank-gain floor guarantees a full-rank selected set. Note the trade-off: it helps at Float16 and can hurt at Float64, where one-step optimality does not survive greedy myopia.\nELU, GELU, tanh — OGA2d or OGASphere. The 1-D dictionary cannot represent more than one transition steepness, which is a structural limitation and not a numerical one; no fit or selection rule repairs it.\nA coarse dictionary, or a cost budget — wrap it in Refined. It lets a few dozen atoms stand in for hundreds of thousands.\n\"Must never fail at any precision\" — TruncatedSVD with OrthogonalProjection.","title":"Choosing a variant"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Per-integrator-behaviour","page":"Usage","text":"All four network integrators share oga_fit. What differs is declared, not reimplemented:\n\nIntegrator Ansatz Symmetry Constructor default\nNonLinear_OneLayer_GML — NoSymmetry OGA1d\nHardcode_int t(1-t), target minus the linear part NoSymmetry OGA1dNormalized\nTime_reversible_OneLayer — MirrorPairs OGA1d\nTime_Reversible_Hardcode t(1-t), target minus the linear part SharedMirrorPairs OGA1d\n\nHardcode_int is the odd one out, and deliberately so: its greedy step ranks candidates by the normalised inner product where the other three used the raw one. Since that choice decides which neurons are picked and hence which basin the Newton solve lands in, each integrator keeps the rule it was tuned with rather than inheriting a single shared default.\n\nTwo further notes. The two boundary-ansatz integrators differ in where the endpoint of the linear part comes from — Hardcode_int uses the last label (i.e. the initial-trajectory integrator's estimate), Time_Reversible_Hardcode uses the cache's endpoint estimate q̃ — and both write that endpoint into the nonlinear solution vector, which the OneLayer variants do not (there the corresponding slot is the momentum, set by initial_trajectory!).\n\nNonLinear_DenseNet_GML has no OGA seed; it uses TrainingMethod or LSGD.","title":"Per-integrator behaviour"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Reading-the-result","page":"Usage","text":"oga_fit can also be called directly, which is how the seed study measures variants without an integrator in the way:\n\nusing NonlinearIntegrators\nconst NI = NonlinearIntegrators\n\nT = Float32\nnodes = T.((0:10) ./ 10)\nw     = NI.simpson_quadrature(10, T)\ny     = T.(cos.(3 .* Float64.(nodes)))\n\nr = oga_fit(OGA1dStable(), x -> max(zero(x), x)^3, nodes, w, y, 4;\n            bias_interval = [-T(π), T(π)], dict_amount = 400)\n\nr.W, r.b, r.c    # hidden weights, hidden biases, output weights (one entry per neuron)\nr.atoms          # dictionary indices selected, in order\nr.neurons        # how many of the 4 were actually placed\nr.gains          # ‖g⊥‖ of each accepted atom — the rank gain it contributed\nr.rejected       # candidates refused for adding no new direction\nr.residual       # final weighted L² residual norm\n\ngains and rejected are the diagnostics worth watching. A gains sequence collapsing towards zero across the four atoms is the fingerprint of the failure this subsystem exists to remove; neurons < S means the loop ran out of atoms that add a new direction and the remainder were filled with zero-weight placeholders.","title":"Reading the result"},{"category":"section","location":"Orthogonal Greedy Algorithm/usage/#Extending","page":"Usage","text":"Adding a component means one type and one method.\n\n# A new dictionary\nstruct MyGrid <: OGADictionary\n    n::Int\nend\nfunction NonlinearIntegrators.oga_atoms(d::MyGrid, bias_interval, dict_amount, ::Type{T}) where {T}\n    # return an (natoms × 2) Matrix{T}: column 1 is w, column 2 is b\nend\nNonlinearIntegrators.oga_label(::MyGrid) = \"mygrid\"      # for the study reports\n\n# A new fit\nstruct MyFit <: OGAFit end\nfunction NonlinearIntegrators._oga_solve(::MyFit, Â::AbstractMatrix{T}, ŷ, qr) where {T}\n    # return a length-size(Â,2) Vector{T}\nend\n\nDefine _oga_solve, not oga_solve: the latter is the guarded wrapper that guarantees a finite, correctly sized result for every fit, and defining it directly would opt out of that guarantee. A new dictionary inherits the no-op NonlinearIntegrators.oga_refine unless wrapped in Refined.\n\nWhatever you add must respect the precision discipline — see Precision. The eltype === T and @inferred assertions in test/unit/oga_kernels.jl sweep every dictionary × selection × fit combination at three precisions, so a new component is covered by adding it to the lists there.","title":"Extending"}]
}
