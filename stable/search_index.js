var documenterSearchIndex = {"docs":
[{"category":"section","location":"Benchmarks/benchmarks/#Benchmarks","page":"Benchmarks","text":"The package ships a benchmark suite (under benchmark/) for the one-layer GML variational integrator NonLinear_OneLayer_GML. It runs each of several test problems through a large grid of integrator configurations and records, for every case, whether the nonlinear solve converged, how accurate the result is, how much the energy drifts, how many nonlinear iterations it took, and how long it ran. The results are written to CSV, summarised in a markdown report, and visualised with a set of plots.\n\nThe suite has three goals:\n\nfind which integrator-parameter configurations work well for each test problem;\nidentify issues in the package that are detrimental to performance;\nidentify robust solver strategies.\n\nThere is also a separate, narrower benchmark/oga_comparison.jl that compares the two OGA initial-guess variants; see the Orthogonal Greedy Algorithm page and benchmark/README.md.","title":"Benchmarks"},{"category":"section","location":"Benchmarks/benchmarks/#What-is-swept","page":"Benchmarks","text":"Each case integrates a problem for exactly 10 time steps; the time span is adapted per case as (0, 10·dt). The sweep spans, per problem:\n\naxis meaning\ntimestep dt integration step size\nprecision working floating-point type (Float16 / Float32 / Float64)\nR Gauss–Legendre quadrature order\nS number of hidden neurons (network width)\nactivation ReLUᵏ (k = 2, 3, 4), ELU, GELU, or tanh\nsolver strategy Newton with Static / Backtracking / StrongWolfe line search, or trust-region DogLeg\nλ Jacobian regularization (regularization_factor)\ninitial guess midpoint (IntegratorExtrapolation), Hermite (HermiteExtrapolation), or previous (NoExtrapolation)\n\nThe test problems (from GeometricProblems.jl) are the harmonic oscillator, the pendulum (a degenerate two-component IODE — it has no lodeproblem), the double pendulum, and the Toda lattice with N = 16.","title":"What is swept"},{"category":"section","location":"Benchmarks/benchmarks/#Modes","page":"Benchmarks","text":"Each per-problem run file takes a mode — quick (default) or full — from its first command-line argument or from the GML_BENCH_PRESET environment variable.\n\naxis full quick\ndt 0.01, 0.1, 1.0, 10.0 0.1, 1.0, 10.0\nprecision Float16, Float32, Float64 Float64, Float32, Float16\nR 4, 8, 16 8 (16 for double pendulum & Toda)\nS 4, 6, 8 4 (8 for double pendulum & Toda)\nactivation ReLU², ReLU³, ReLU⁴, ELU, GELU, tanh GELU, tanh\nsolver Newton/{Static, Backtracking, StrongWolfe}, DogLeg DogLeg\nλ 0.0, 1e-7, 1e-5, 1e-3, 16√eps(T) 16√eps(T)\ninitial guess midpoint, Hermite, previous midpoint\nmax_iterations 10000 100\n\nquick is roughly 18 cases per problem (seconds to minutes each — the Toda lattice is the slowest because of its N = 16 state and larger network); full is on the order of tens of thousands of cases per problem (hours). Results are flushed to CSV per case, so an interrupted full run keeps its partial output.\n\nThe 16√eps(T) regularization scales the Jacobian-diagonal damping with the working precision: ≈2.4e-7 at Float64, ≈5.5e-3 at Float32, and 0.5 at Float16. The last is large and tends to over-damp half precision; note, however, that at half precision the ReLUᵏ basis is ill-conditioned and diverges independently of λ, whereas tanh still converges — the accuracy limit there is the precision, not the regularization.","title":"Modes"},{"category":"section","location":"Benchmarks/benchmarks/#Metrics","page":"Benchmarks","text":"For each case the suite records:\n\nstatus — ok, or a failure class (singular, diverged, nonfinite, or the short name of any other exception raised);\nref_err — the relative max-norm error of the final state against a reference, which is a Gauss(8) integration at Float64 using the smallest timestep in the sweep (over the same 10-step horizon);\nham_drift — the maximum relative drift of the Hamiltonian over the run;\niterations — the nonlinear-solver iteration count of the final step;\nsolve_secs / total_secs — the summed nonlinear-solve time and the wall-clock time of the run.","title":"Metrics"},{"category":"section","location":"Benchmarks/benchmarks/#Running","page":"Benchmarks","text":"Instantiate the benchmark environment (it devs the package):\n\njulia --project=benchmark -e 'using Pkg; Pkg.develop(PackageSpec(path=pwd())); Pkg.instantiate()'\n\nRun one or more problems (mode defaults to quick):\n\njulia --project=benchmark benchmark/run_harmonic_oscillator.jl\njulia --project=benchmark benchmark/run_pendulum.jl\njulia --project=benchmark benchmark/run_double_pendulum.jl\njulia --project=benchmark benchmark/run_toda_lattice.jl full   # full sweep\n\nEach run writes benchmark/results/<problem>_<mode>.csv, a benchmark/results/<problem>_<mode>.md report, and PNG plots. Finally, aggregate every CSV present into a combined report:\n\njulia --project=benchmark benchmark/report.jl\n\nwhich writes benchmark/results/onelayer_gml_benchmark.md. The reporting step reads the CSVs, so a report can be regenerated (or restyled) without re-running the sweep. All benchmark/results/ contents are git-ignored.","title":"Running"},{"category":"section","location":"Benchmarks/benchmarks/#Outputs","page":"Benchmarks","text":"The CSV has one row per case with the columns\n\nproblem, T, dt, steps, R, S, activation, solver, linesearch, initial_guess,\nlambda, status, ref_err, ham_drift, iterations, solve_secs, total_secs\n\nThe markdown report contains a status breakdown, convergence/robustness tables (by solver strategy, initial-guess strategy, precision, and problem), the best configuration found per problem, and failure hot-spots. It embeds the plots:\n\nconvergence — success-rate bars per solver strategy, and a solver × precision heatmap (red = not converged, green = converged); the combined report also draws a success-rate bar per problem;\naccuracy, energy drift, run time, and nonlinear iterations — each as a scatter versus the timestep. A per-problem report colours the dots by precision; the combined report colours them by problem, so the four problems stay distinguishable.","title":"Outputs"},{"category":"section","location":"Benchmarks/benchmarks/#Results","page":"Benchmarks","text":"The figures on this page are not committed: they are regenerated at documentation-build time by a fresh quick run over the four problems (driven from docs/make.jl), so they track the current package. The narrative and tables are kept as an illustrative reference from one representative run — the numbers are not a fixed reference and may not match the freshly generated figures exactly. That run used the DogLeg solver, the midpoint initial guess, and the precision-scaled regularization λ = 16√eps(T).\n\nResults are organised as a summary across all problems followed by one section per problem (harmonic oscillator, pendulum, double pendulum, Toda lattice). The summary scatters are coloured by problem; each per-problem section shows that problem's scatters coloured by precision.","title":"Results"},{"category":"section","location":"Benchmarks/benchmarks/#Summary-across-all-problems","page":"Benchmarks","text":"Across the four problems (72 cases in the representative run), 48 converged. Convergence is dominated by precision: half precision is by far the least robust.\n\nprecision cases converged success median ref_err median ham_drift\nFloat16 24 5 21% 7.15e-03 7.81e-03\nFloat32 24 21 88% 1.90e-04 1.04e-04\nFloat64 24 22 92% 2.22e-04 1.67e-04\n\nSuccess rate broken down by problem, by solver strategy, and by solver × precision:\n\n(Image: Convergence success rate by problem)\n\n(Image: Convergence success rate by solver strategy)\n\n(Image: Convergence success rate by solver and precision)\n\nAccuracy, energy drift, run time and nonlinear-iteration counts versus the timestep, with all four problems overlaid and coloured by problem (each dot is one converged case). Accuracy and energy conservation degrade sharply as the timestep grows; at dt = 10 the 10-step horizon is far too coarse and the relative error is O(1).\n\n(Image: Accuracy versus timestep)\n\n(Image: Energy drift versus timestep)\n\n(Image: Run time versus timestep)\n\n(Image: Nonlinear iterations versus timestep)\n\nThe best (lowest ref_err) converged configuration found for each problem:\n\nproblem best ref_err T dt network iguess / λ\nharmonic_oscillator 3.77e-13 Float64 0.1 R8 S4 ReLU³ midpoint, λ=2.4e-7\npendulum 1.03e-07 Float64 0.1 R8 S4 ReLU³ midpoint, λ=2.4e-7\ndouble_pendulum 1.28e-07 Float64 0.1 R16 S8 tanh midpoint, λ=2.4e-7\ntoda_lattice 1.55e-08 Float64 0.1 R16 S8 tanh midpoint, λ=2.4e-7\n\nThe failures concentrate at half precision (across all timesteps) and at the largest timestep dt = 10 (across all precisions) — consistent with the accuracy plot.","title":"Summary across all problems"},{"category":"section","location":"Benchmarks/benchmarks/#Harmonic-oscillator","page":"Benchmarks","text":"The simplest test problem: a single linear oscillator. It reaches the best accuracy of the four (ref_err down to O(1e-13) at dt = 0.1, Float64), and the precision split is the clearest — Float64 and Float32 track each other closely while Float16 is limited by the working precision. Each dot below is a converged case, coloured by precision.\n\n(Image: Accuracy versus timestep — harmonic oscillator)\n\n(Image: Energy drift versus timestep — harmonic oscillator)\n\n(Image: Run time versus timestep — harmonic oscillator)\n\n(Image: Nonlinear iterations versus timestep — harmonic oscillator)\n\n(Image: Convergence heatmap — harmonic oscillator)","title":"Harmonic oscillator"},{"category":"section","location":"Benchmarks/benchmarks/#Pendulum","page":"Benchmarks","text":"A degenerate two-component IODE (ϑ: p₁ = ml²q₂, p₂ = 0; it has no lodeproblem), included deliberately to stress the nonlinear solve. It is nonlinear and less accurate than the harmonic oscillator, and its half-precision cases are the most fragile.\n\n(Image: Accuracy versus timestep — pendulum)\n\n(Image: Energy drift versus timestep — pendulum)\n\n(Image: Run time versus timestep — pendulum)\n\n(Image: Nonlinear iterations versus timestep — pendulum)\n\n(Image: Convergence heatmap — pendulum)","title":"Pendulum"},{"category":"section","location":"Benchmarks/benchmarks/#Double-pendulum","page":"Benchmarks","text":"A four-dimensional chaotic system. Quick mode uses a larger network (R = 16, S = 8) than the two simple problems; tanh gives the best accuracy here.\n\n(Image: Accuracy versus timestep — double pendulum)\n\n(Image: Energy drift versus timestep — double pendulum)\n\n(Image: Run time versus timestep — double pendulum)\n\n(Image: Nonlinear iterations versus timestep — double pendulum)\n\n(Image: Convergence heatmap — double pendulum)","title":"Double pendulum"},{"category":"section","location":"Benchmarks/benchmarks/#Toda-lattice-(N-16)","page":"Benchmarks","text":"The largest problem, with a 16-dimensional state and a correspondingly larger network (R = 16, S = 8 in quick mode). It is the slowest to run — its run-time scatter sits above the other three problems in the summary.\n\n(Image: Accuracy versus timestep — Toda lattice)\n\n(Image: Energy drift versus timestep — Toda lattice)\n\n(Image: Run time versus timestep — Toda lattice)\n\n(Image: Nonlinear iterations versus timestep — Toda lattice)\n\n(Image: Convergence heatmap — Toda lattice)","title":"Toda lattice (N = 16)"},{"category":"section","location":"Benchmarks/benchmarks/#Extending","page":"Benchmarks","text":"To add a problem, copy one of the run_*.jl files and supply a build_prob(T, timespan, timestep) closure returning an AbstractProblemIODE at element type T, plus a hamiltonian(t, q, p, params) closure; then call run_sweep. Per-problem axis overrides (as the double pendulum and Toda lattice use for R and S) are passed as the Rs / Ss keyword arguments to run_sweep. The shared engine, presets, and reporting live in benchmark/gml_benchmark_common.jl and benchmark/gml_report.jl.","title":"Extending"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#Orthogonal-Greedy-Algorithm-Initial-Guess","page":"Orthogonal Greedy Algorithm","text":"The network integrators in this package (NonLinear_OneLayer_GML, Hardcode_int, Time_reversible_OneLayer, Time_reversible_Hardcode_int) solve, at every time step, a nonlinear system for the parameters of a shallow neural network that represents the trajectory between two discrete nodes. That nonlinear (Newton) solve needs a good initial guess. This guess is produced by an Orthogonal Greedy Algorithm (OGA) [1], a form of greedy training for shallow networks [2]: candidate neurons are drawn from a fixed dictionary, the neuron most correlated with the current residual is added one at a time, and the output weights are refit by (quadrature-weighted) least squares after each addition.\n\nThis page documents why the OGA fit used to be pinned to Float64, why that pin was unnecessary for accuracy but was necessary given the original formulation, and how the fit was reformulated so that the whole seed runs at the solver's working precision T — including Float32 (the GPU-native type) and Float16.","title":"Orthogonal Greedy Algorithm Initial Guess"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#The-problem-with-the-original-formulation","page":"Orthogonal Greedy Algorithm","text":"Per spatial dimension, the OGA fit repeatedly solves a linear least-squares problem for the output weights x of the currently selected neurons,\n\nmin_x  sum_j w_j Bigl( sum_i x_i Phi_ij - y_j Bigr)^2 \n\nwhere each row of Phi is a dictionary atom sampled at the quadrature nodes, w_j  0 are the quadrature weights and y_j is the fit target at node j. The original code solved this through the normal equations\n\nGx = Phioperatornamediag(w)Phi^top x = Phioperatornamediag(w)y \n\nForming the Gram matrix G = Phioperatornamediag(w)Phi^top squares the condition number, kappa(G) = kappa(Phi)^2 [3], [4]. A matrix can only be solved reliably while its condition number stays below 1varepsilon, where varepsilon is the unit roundoff of the working type (eps(T)). Squaring the condition number therefore halves the number of usable digits, and in reduced precision the Gram matrix becomes numerically singular: two dictionary neurons whose biases round to nearly the same low-precision value produce nearly identical columns of Phi, so G is (near-)rank-deficient [5]. This is why the seed used to be assembled in Float64.\n\nCrucially, the extra Float64 precision brought no accuracy benefit: the OGA result is only a seed, immediately rounded back to T when stored in the cache, and the final accuracy is set by the working-precision Newton solve. The Float64 island only ever bought robustness of the ill-conditioned Gram solve — a robustness that is unnecessary once the solve itself is well-conditioned.\n\nThree hard-coded guard-rail constants encoded the Float64 assumption and were silently ineffective in reduced precision:\n\na dictionary-norm floor dict_norms < 1e-12, which sits below eps(Float32) approx 12times 10^-7 and so never fires;\nTikhonov ridges G + 1e-12·I and G + 1e-14·I, which round away entirely below eps(Float32);\na bias grid lo:(hi-lo)/dict_amount:hi that threw ArgumentError: range step cannot be zero in Float16 when a large dict_amount overflowed Float16(dict_amount) to Inf.","title":"The problem with the original formulation"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#The-reformulated-algorithm","page":"Orthogonal Greedy Algorithm","text":"The fit is now solved by a QR factorization of the sqrtw-scaled design matrix rather than the normal equations. Folding the (positive) weights into a row scaling turns the weighted problem into an ordinary least-squares problem,\n\nmin_x  bigllVert hatAx - haty bigrrVert_2^2  qquad\nhatA = operatornamediag(sqrtw)Phi^top  qquad\nhaty = operatornamediag(sqrtw)y \n\nsolved by QR [6], [3]. Because QR works directly on hatA, the accuracy is governed by kappa(Phi) instead of kappa(Phi)^2 — roughly doubling the resolvable conditioning at every precision, which is exactly the Float64→Float32 gap. The Gram matrix is never formed. This is implemented by NonlinearIntegrators.weighted_lstsq.\n\nThree further ingredients make the seed robust and precision-generic:\n\nPrecision-scaled guard rails. The hard-coded constants are replaced by formulas that track the working precision:\nthe norm floor becomes sqrtvarepsilonlVertcdotrVert_max (NonlinearIntegrators.oga_norm_floor);\nthe Tikhonov ridge becomes lambda = Cvarepsilonoperatornametr(G)n with a modest safety factor C (NonlinearIntegrators.oga_tikhonov).\nThe ridge is applied only as a fallback: weighted_lstsq uses the plain QR solve whenever it is finite (so the well-conditioned Float64/Float32 atom choice is unchanged and the greedy residual is unperturbed) and augments the design matrix with a sqrtlambdaI block only when the plain solve returns a non-finite result — the genuinely rank-deficient Float16 case.\nA coherence guard. After a neuron is selected, dictionary atoms whose quadrature-weighted L² coherence with it exceeds 1 - sqrtvarepsilon are blocked from future selection. This keeps the selected neurons linearly independent. It is inert at Float64/Float32 (distinct atoms have coherence well below the threshold) and only bites at Float16, where many grid biases collapse onto the same value.\nA safe bias grid. NonlinearIntegrators.bias_grid builds the grid from an integer-indexed range and casts to T, so a large dict_amount cannot overflow the step to zero.\n\nThe greedy selection itself is deliberately left unchanged (it still maximizes the raw inner product between the dictionary and the residual). Normalizing the dictionary before selection would change which neurons are picked and steer the Newton solve into a different — empirically worse — solution basin; normalization is therefore used only to measure coherence for the guard above.","title":"The reformulated algorithm"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#Selecting-the-algorithm-and-comparing-the-two","page":"Orthogonal Greedy Algorithm","text":"The reformulated algorithm is the default (OGA1d). The previous algorithm is kept as a selectable alternative, OGA1d_Legacy, for NonLinear_OneLayer_GML:\n\n# default: working-precision QR fit\nNonLinear_OneLayer_GML(basis, quadrature; ...)\n# previous: Float64 island + normal equations\nNonLinear_OneLayer_GML(basis, quadrature; ..., initial_guess_method = OGA1d_Legacy())\n\nbenchmark/oga_comparison.jl runs both end-to-end (OGA seed + Newton solve) on the harmonic oscillator across problems of increasing complexity — the time-step length dt (hence the number of steps) and the number of neurons S (hence the number of parameters) — at Float64, Float32 and Float16. In summary: where both converge they reach the same accuracy (the seed is only a warm start), but the legacy Gram solve already goes singular for the larger network (S = 8) and fails across the board at Float16, whereas the QR seed stays finite and lets the solve proceed. See benchmark/README.md for how to run it.","title":"Selecting the algorithm and comparing the two"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#A-didactic-Float16-example","page":"Orthogonal Greedy Algorithm","text":"The following self-contained example reproduces, in miniature, the failure that used to force the Float64 island. We build four ReLU³ neurons — two of them with almost identical biases (0.300 and 0.305), the situation that makes the Gram matrix ill-conditioned — and fit a target that is a known combination of them. We solve the least-squares problem both the old way (normal equations / Gram matrix) and the new way (QR on the sqrtw-scaled design matrix), in Float64 and in Float16.\n\nusing LinearAlgebra\n\nσ(x) = max(zero(x), x)^3          # ReLU³ activation\n\nfunction setup(::Type{T}) where {T}\n    t = T.(range(0, 1; length = 9))                 # quadrature nodes on [0,1]\n    w = fill(one(T) / 9, 9)                         # (toy) positive weights\n    biases = T[0.300, 0.305, -0.20, 1.50]           # atoms 1 and 2 nearly identical\n    Φ = reduce(vcat, (permutedims(σ.(t .+ b)) for b in biases))   # (natoms × nnodes)\n    y = T(0.7) .* σ.(t .+ T(0.30)) .- T(0.4) .* σ.(t .- T(0.20))  # target\n    return t, w, Φ, y\nend\n\n# OLD: normal-equations (Gram) solve\nfunction gram_solve(Φ, w, y)\n    G   = Φ * (w .* Φ')\n    rhs = Φ * (w .* y)\n    return G, G \\ rhs\nend\n\n# NEW: QR on the √w-scaled design matrix\nfunction qr_solve(Φ, w, y)\n    sw = sqrt.(w)\n    Â  = sw .* Φ'\n    return Â, Â \\ (sw .* y)\nend\nnothing # hide\n\nIn Float64 both methods recover the true weights [0.7, 0, -0.4, 0], and we can see the condition number relationship kappa(G) = kappa(hatA)^2 directly:\n\nt, w, Φ, y = setup(Float64)\nG, xg = gram_solve(Φ, w, y)\nÂ, xq = qr_solve(Φ, w, y)\n\nfit_err(x) = sqrt(sum(w .* (Φ' * x .- y) .^ 2))\n\nprintln(\"cond(G)  = \", cond(G))\nprintln(\"cond(Â)² = \", cond(Â)^2)\nprintln(\"Gram x   = \", xg, \"   fit-err = \", fit_err(xg))\nprintln(\"QR   x   = \", xq, \"   fit-err = \", fit_err(xq))\n\nIn Float16, the achievable condition number is only about 1texttteps(Float16) approx 10^3. The Gram condition number is far beyond that, so the Gram solve returns finite garbage — the two near-duplicate atoms receive huge weights of opposite sign — while the QR solve stays bounded and close to the truth:\n\nt, w, Φ, y = setup(Float16)\nG, xg = gram_solve(Φ, w, y)\nÂ, xq = qr_solve(Φ, w, y)\n\nfit_err(x) = sqrt(sum(w .* (Φ' * x .- y) .^ 2))\n\nprintln(\"1/eps(Float16) = \", 1 / eps(Float16))\nprintln(\"cond(G)        = \", Float64(cond(Float64.(G))))\nprintln(\"cond(Â)        = \", Float64(cond(Float64.(Â))))\nprintln(\"Gram x = \", Float64.(xg), \"   fit-err = \", Float64(fit_err(xg)))\nprintln(\"QR   x = \", Float64.(xq), \"   fit-err = \", Float64(fit_err(xq)))\n\nThe QR result matches what the shipped fit produces (weighted_lstsq implements exactly this QR solve, plus the ridged fallback for the rank-deficient case):\n\nusing NonlinearIntegrators\nt, w, Φ, y = setup(Float16)\nprintln(\"weighted_lstsq x = \", Float64.(NonlinearIntegrators.weighted_lstsq(Φ, w, y)))\n\nIn the full integrator the Float16 Gram garbage does not merely produce a poor seed: fed into the Newton solve it makes the parameter Jacobian singular (two nearly identical neurons are nearly linearly dependent), which previously surfaced as a SingularException or a NaN. The QR reformulation, the coherence guard and the ridged fallback together keep the seed finite and well-behaved, so the run proceeds at the working precision.\n\nThe helper functions weighted_lstsq, oga_norm_floor, oga_tikhonov and bias_grid are documented (with their full docstrings) in the API listing on the home page.","title":"A didactic Float16 example"},{"category":"section","location":"Orthogonal Greedy Algorithm/OGA/#References","page":"Orthogonal Greedy Algorithm","text":"V. N. Temlyakov. Greedy approximation. Acta Numerica 17, 235–409 (2008).\n\n\n\nJ. W. Siegel, Q. Hong, X. Jin, W. Hao and J. Xu. Greedy training algorithms for neural networks and applications to PDEs. Journal of Computational Physics 484, 112084 (2023).\n\n\n\nG. H. Golub and C. F. Van Loan. Matrix Computations. 4th Edition (Johns Hopkins University Press, Baltimore, 2013).\n\n\n\nN. J. Higham. Accuracy and Stability of Numerical Algorithms. 2nd Edition (Society for Industrial and Applied Mathematics, 2002).\n\n\n\nN. J. Higham and T. Mary. Mixed precision algorithms in numerical linear algebra. Acta Numerica 31, 347–414 (2022).\n\n\n\nÅ. Björck. Numerical Methods for Least Squares Problems (Society for Industrial and Applied Mathematics, 1996).\n\n\n\n","title":"References"},{"category":"section","location":"Variational Integrator with Symbolic Expression/VISE/#Basic-Usage","page":"-","text":"At first, we have to load several packages:\n\nusing Symbolics\nusing CompactBasisFunctions\nusing NonlinearIntegrators\nusing QuadratureRules\nusing GeometricProblems\nusing GeometricIntegrators\n\nAdditional package would be required if we want to use Nested Sindy expression.\n\nusing Symbolics\n\nLike what we would do in GeometricIntegrators.jl, we first define the problem we want to solve. The type of problem here is restricted to Lagrangian ordinary equations, i.e. models have a regular Lagrangian and its Lagrangian equation of motions.\n\nT = 100.0\nh_step = 2.0\nHHlode = GeometricProblems.HenonHeilesPotential.lodeproblem([0.1,0.1],[0.1,0.1],timespan = (0,T),timestep = h_step)\n\nFor each dimension of generalized coordinates q = (q<sub>1</sub>, q<sub>2</sub>), we prescribe a expression that approximates the coordinates.\n\n@variables W1[1:4] W2[1:4] t\nq₁_expr = W1[1] *cos(W1[2]* t + W1[3]) + W1[4]\nq₂_expr = W2[1] *cos(W2[2]* t + W2[3]) + W2[4]\n\nthen define a basis object for variational Galerkin integrator, in this case, a Polynomial-Radial basis for the two dimension problem.\n\nPRB = PR_Basis{Float64}([q₁_expr,q₂_expr], [W1,W2], t,2)\n\nalong with a chosen quadrature rule, and initial guess for the parameters in the expression, we define the integrator, and start the game :)\n\nPR_Int = PR_Integrator(PRB, QGau8,[[0.14831,1.0,-0.64812,- 0.018712],[0.14298,- 0.97215,0.7615,-0.0013983]]) \nPR_sol = integrate(HHlode, PR_Int)\n\nOne good thing about continuous Galerkin variational integrators is that you could record the coordinates values between two discrete steps for free!\n\n\nHHlode_ref = GeometricProblems.HenonHeilesPotential.lodeproblem([0.1,0.1],[0.1,0.1],timespan = (0,TT),timestep = h_step/50)\nref_sol = integrate(HHlode_ref, ImplicitMidpoint())\n\nusing Plots\nsol_mat = hcat(PR_sol[2][:,2:end,1]'...)[1,:]\nplot(collect(h_step/40:h_step/40:TT),sol_mat,label = \"Continuous Solution\")\nscatter!(collect(0:h_step:TT),collect(PR_sol[1].q[:,1]),label = \"Discrete Solution\")\nplot!(collect(0:h_step/50:TT),collect(ref_sol[1].q[:,1]), label = \"Reference\")\n\n(Image: HenonHeilies)","title":"Basic Usage"},{"category":"section","location":"#NonlinearIntegrators","page":"Home","text":"<!– Julia Package aims to generalize Galerkin variational integrator with polynomial basis to nonlinear basis. –>","title":"NonlinearIntegrators"},{"category":"type","location":"#NonlinearIntegrators.CGVI_standard","page":"Home","text":"Continuous Galerkin Variational Integrator.\n\nb: weights of the quadrature rule\nc: nodes of the quadrature rule\nx: nodes of the basis\nm: mass matrix\na: derivative matrix\nr₀: reconstruction coefficients at the beginning of the interval\nr₁: reconstruction coefficients at the end of the interval\n\n\n\n\n\n","title":"NonlinearIntegrators.CGVI_standard"},{"category":"method","location":"#NonlinearIntegrators.bias_grid-Union{Tuple{T}, Tuple{Any, Any, Integer, Type{T}}} where T","page":"Home","text":"bias_grid(lo, hi, n, ::Type{T}) -> Vector{T}\n\nUniform grid of n + 1 bias values from lo to hi in precision T.\n\nThe coordinates are generated from an integer-indexed range in Float64 and then cast to T, so a large n cannot overflow the step to zero in reduced precision (the Float16 trap where T(n) overflows to Inf and (hi - lo)/n evaluates to zero, previously throwing ArgumentError: range step cannot be zero). Only the grid coordinates touch Float64; the seed's dictionary and solve run entirely in T.\n\n\n\n\n\n","title":"NonlinearIntegrators.bias_grid"},{"category":"method","location":"#NonlinearIntegrators.initial_params!-Tuple{GeometricIntegratorsBase.GeometricIntegrator{var\"#s49\", PT, CT} where {var\"#s49\"<:NonLinear_OneLayer_GML, PT<:GeometricBase.AbstractProblem, CT<:GeometricIntegratorsBase.CacheDict{PT, var\"#s49\"}}, OGA1d_Legacy}","page":"Home","text":"initial_params!(int, ::OGA1d_Legacy)\n\nLegacy OGA initial guess for NonLinear_OneLayer_GML, kept as a selectable alternative to the default OGA1d for comparison. This is the pre-refactor algorithm: the dictionary and the greedy least-squares fit are assembled in Float64 (a \"double-precision island\"), the output weights are obtained from the normal equations Gk \\ rhs, and the result is rounded into the working-precision cache. See the \"Orthogonal Greedy Algorithm\" section of the documentation for why this was replaced by a working-precision QR fit. Select it with NonLinear_OneLayer_GML(...; initial_guess_method = OGA1d_Legacy()).\n\n\n\n\n\n","title":"NonlinearIntegrators.initial_params!"},{"category":"method","location":"#NonlinearIntegrators.initial_trajectory!-Tuple{Any, Any, Any, GeometricIntegratorsBase.GeometricIntegrator, GeometricIntegratorsBase.Extrapolation}","page":"Home","text":"initial_trajectory!(sol, history, params, int, initial_trajectory)\n\nInitial trajectory for the NonLinear_OneLayer_Lux integrator.\n\n\n\n\n\n","title":"NonlinearIntegrators.initial_trajectory!"},{"category":"method","location":"#NonlinearIntegrators.oga_norm_floor-Union{Tuple{T}, Tuple{Type{T}, Any}} where T","page":"Home","text":"oga_norm_floor(::Type{T}, ref) -> T\n\nAmplitude-scale floor below which a dictionary-atom norm is treated as numerically zero (so normalization is skipped rather than dividing by noise). Scales with sqrt(eps(T)) * ref, i.e. relative to the largest atom ref and to the working precision: a norm smaller than this cannot be inverted reliably in T.\n\nReplaces the hard-coded absolute 1e-12 guard, which sat below eps(Float32) and so never fired in reduced precision.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_norm_floor"},{"category":"method","location":"#NonlinearIntegrators.oga_tikhonov-Union{Tuple{AbstractMatrix{T}}, Tuple{T}} where T","page":"Home","text":"oga_tikhonov(G; C = 100) -> eltype(G)\n\nScale- and precision-relative Tikhonov floor C * eps(T) * tr(G)/n for stabilizing a Gram / normal-equations solve G \\ rhs at working precision T = eltype(G). Relative to the mean diagonal tr(G)/n, so the effective condition-number cap tracks the precision; C is a modest safety factor.\n\nReplaces the hard-coded absolute 1e-12 / 1e-14 ridges, which round away entirely below eps(Float32). Provided as a fallback for a regularized normal-equations variant; the OGA fit itself uses weighted_lstsq, which avoids forming G.\n\n\n\n\n\n","title":"NonlinearIntegrators.oga_tikhonov"},{"category":"method","location":"#NonlinearIntegrators.weighted_lstsq-Union{Tuple{T}, Tuple{AbstractMatrix{T}, AbstractVector{T}, AbstractVector{T}}} where T","page":"Home","text":"weighted_lstsq(Φ, w, y; C = 100) -> Vector\n\nSolve the (ridged) quadrature-weighted least-squares fit for the output weights x,\n\nminₓ  Σⱼ wⱼ (Σᵢ xᵢ Φ[i,j] − yⱼ)²  +  λ‖x‖² ,\n\nwhere each row of Φ is a dictionary atom sampled at the quadrature nodes, w are the (positive) quadrature weights, and y is the fit target at those nodes.\n\nSolved by QR on the √w-scaled design matrix rather than the normal equations Φ diag(w) Φᵀ, so accuracy is governed by κ(Φ) instead of κ(Φ)² — the difference that lets the fit run in reduced precision without a rank-deficient Gram matrix.\n\nThe plain QR solve is used whenever it is finite (so the Float64/Float32 atom choice is byte-for-byte the old Gram solution and the greedy residual is unperturbed). Only if it returns a non-finite result — a genuinely rank-deficient design matrix, i.e. the Float16 case — is the fit retried with a √λ·I augmentation, where the ridge λ = C · eps(T) · tr(ÂᵀÂ)/natoms is the precision-scaled Tikhonov floor (see oga_tikhonov) that keeps the solution bounded and the seed finite.\n\n\n\n\n\n","title":"NonlinearIntegrators.weighted_lstsq"}]
}
